# ochakai 設計ドキュメント 0073: 検索が何を融合し、埋め込みがいつ効くか

Status: Accepted(2026-08-03)。**検索と埋め込みの現行ドキュメント。**
[0020](0020-attachment-search.md) を Superseded にし、
[0053](0053-embeddings-by-default.md) を Superseded にする。
**二つが決めたことは何一つ変えない**(§0)。埋め込みの**設定面** —
§1.4 の off スイッチ、§2 の次元、§4 のモデル選択がどう書かれるか — は
[0078](0078-one-variable-says-how-it-embeds.md) が改訂する。
スコアの床を持たないことは
[0068](0068-how-a-face-is-added-and-removed.md) §3、`context` の `hits` が
順位に徹することは [0067](0067-four-faces-and-what-they-decline.md) §4、
住所で絞る `prefix` は [0041](0041-path-scoped-search.md) が持つ
Date: 2026-08-03

## 0. この記録が決めたこと

**二つの記録が一つの絵であること。** issue
[#471](https://github.com/na0fu3y/ochakai/issues/471) が始めた統合の三回目
である。決定は一つも動かない。

二つは互いの前提だった。0053 §5 は「添付の昇格が実際に効くための最後の
一片は、埋め込みが既定になることだった」と書き、0020 は「埋め込みが構成
されたときだけ」を前提に書かれている。**検索が何を融合するかと、埋め込みが
いつ効くかは、片方だけ読んでも答えが出ない。**

## 1. Google Cloud の上では、埋め込みが既定である

**この製品でいちばん効く機能が、いちばん奥に置いてあった。** 日本語の字句
検索は trigram 索引が二文字のパターンを引けないためスキャンで答えることに
なり、答えは「埋め込みを有効にすること」だと ROADMAP 自身が書いていた。
その答えに至るには、長いデプロイガイドの中ほどにある任意設定の節を読み、
API を有効にし、IAM ロールを付け、環境変数を置き、`reembed` を打つ必要が
あった。

**中核の想定利用者(日本語で書かれた知識ベースを持つチーム)にとって、
既定の検索は最良の検索ではなかった。** それが既定だったのは、埋め込みが
外部 API を呼ぶからであって、要らないからではない。

### 1.1 プロジェクトは設定ではなく発見する

`OCHAKAI_VERTEX_PROJECT` が無いとき、メタデータサーバに自分がどのプロジェクト
で動いているかを訊き、その答えで埋め込みを有効にする。Cloud SQL IAM 認証が
短命トークンを取っているのと同じ出所であり、**新しく持つものは何も無い**。

メタデータサーバが無い環境 — 手元の開発機、CI、Google Cloud の外 — は何も
変わらない。ADC の資格情報からプロジェクトを取る案は**採らない**:
手元の `gcloud auth application-default login` まで拾って、開発機が黙って
課金を始める。メタデータサーバは「Google Cloud の上で動いている」をそのまま
意味する。

### 1.2 使えるかどうかを決めるのは IAM であって、設定ではない

**ochakai は要求し、IAM が答える。** サービス ID に `roles/aiplatform.user`
が無ければ Vertex AI は答えず、そのデプロイに埋め込みは無い。設定を増やさずに
済むのはこの性質のためで、[0065](0065-identity-and-provenance.md) §1 の
「認可を持たない」と同じ考え方である — 誰が何をしてよいかを製品が二重に
持たない。

答えは**起動時に一度だけ、一語の埋め込みで**訊く。訊かずに走ると、権限の
無いデプロイは書き込みのたびに失敗を記録し続けることになる。呼び出しは
10 秒で打ち切る — cold start は API の不応答を待つ場所ではない。

### 1.3 発見された既定は落ちてよい。名指しされた設定は落ちない

| プロジェクトの出所 | Vertex AI / pgvector が使えないとき |
|---|---|
| 発見(既定) | 警告を一行出し、**字句検索だけで動く** |
| `OCHAKAI_VERTEX_PROJECT`(名指し) | **起動しない** |

名指しした利用者は意味的検索を頼んでおり、黙って劣化した検索を返されるより
なぜ無いのかを知りたい。発見された既定は頼まれたものではないので、知識を
配ることを止める理由にはならない — **落ちた先は、この決定の前にその利用者が
得ていたものである。** 同じ区別が pgvector にも及ぶ。

### 1.4 off はただ一つ、`OCHAKAI_EMBEDDINGS=off`

`OCHAKAI_VERTEX_PROJECT` の不在は、もう off を意味しない。`on` と `off` 以外の
綴りは**設定エラーとして拒否する** — `false` と書いて有効になったデプロイは、
頼んでいない課金を書き込みのたびに払うことになる。**推測して外す方向がまずい
種類の設定である。**

## 2. 次元の変更は製品が行う — ベクトルは導出物だからである

`OCHAKAI_EMBEDDING_DIM` が保存されている列の幅と違うとき、ochakai はベクトルの
テーブルを作り直す。かつてここは起動を拒否し、`psql` で `DROP TABLE` を手で
打てと言っていた — **製品の中で唯一、スキーマ操作を利用者にやらせていた箇所**
であり、この機能が advanced に見える理由になっていた。拒否そのものは正しかった
(幅の違う列に書けば全部壊れる)が、製品が自分でできることだった。

**すべてのベクトルは、それが指すオブジェクトから再計算できる。** だからこそ
ベクトルのテーブルは番号付き migration の外にあり、`reembed` が存在する。
誰も問い合わせない空間を捨てて失われるのは、詰め直すための埋め込み呼び出し
だけで、知識は触れられない。

**詰め直しは自動でやらない。** 作り直したテーブルは空で、埋めることは利用者の
口座から支払うことである。作り直したときはログの一行が `ochakai reembed` を
名指しする。その間も**答えは返る** — 融合は片側が欠けても成立する。

幅が同じでモデルだけ変わった場合、古い行は消さない(§4)。

## 3. 検索は三本のリストを融合する

- **字句** — trigram 類似度と substring 一致。**ファイル名もこのハイスタックに
  入る**ので、`seeds` や `q3-report` のような断片で、それを持つ concept が
  当たる。埋め込みの無いデプロイにもそのまま効き、バックフィル不要で全ファイル
  に即座に働く。
- **concept のベクトル。**
- **ファイルのベクトル** — 書き込み時に埋め込み、検索では concept ごとに最良の
  一件を取って concept のヒットに写像する。

三本を RRF で融合する。**本文でもファイルでも当たる concept は両方のリストから
寄与を得て上がる** — 根拠ごと当たる concept が最初に来るのは意図どおりである。

**変えない原則が二つある。**

1. **ファイルは独立して返らない。** 検索がヒットするのは所有する concept で
   あり、ファイルはその中のメタデータとして現れる。ファイル自体を検索結果と
   して返すかは「ファイルが一級か」ではなく「検索面が何を返すか」の問いで、
   ここでは決めない。
2. **ochakai はファイルを解釈しない。** OCR もキャプションもパースもしない。
   **埋め込みは決定的なエンコーダであって解釈ではない**ので、この線は越えて
   いない。ファイルを読んで本文に書き戻せば、それは本文として検索に載る。

## 4. モデルと次元は、運用者の明示的な判断である

画像と PDF のベクトルを持てるかは埋め込みモデルによる。ファイル入力を取れる
モデル(`gemini-embedding-2` 系)ではバイト列を埋め込み、取れないモデルでは
静かにスキップして**ファイル名の字句一致だけで見つかる**。テキストファイルは
どのモデルでも本文を埋め込む。

**既定のモデルは変えない。** ベクトルはクエリと同じ空間になければ比較できない
ので、画像と PDF を検索に載せるには知識ベース全体をそのモデルで運用することに
なる。既定を切り替えれば既存デプロイの保存済みベクトルが全て別空間の遺物に
なるため、移行は運用者が決める。

**既存ファイルのバックフィルはしない。** 埋め込みは書き込み時のみ、という
姿勢のままである — 後から有効にしたデプロイで埋め込まれるのは以後の書き込み
だけで、詰め直したければ `reembed` がある。字句のファイル名検索は即座に全件に
効くので、「見つからない」ことはない。

## 5. 何を差し出しているか

- **既定パスが Vertex AI に依存する。** [0003](0003-gcp-only.md) が守って
  いるのは secret-zero であって依存の数ではなく、Vertex AI の認証も ADC
  なので**持つ秘密は増えない**。増えるのは「本番で動かす」の敷居である。
- **書き込みが支出になる。** 認可が無い(0065 §1)ので到達できる誰もが書け、
  書き込みは埋め込み呼び出しを生む。逃げ道は二つあり、どちらも明示的である —
  ロールを与えない、または `OCHAKAI_EMBEDDINGS=off`。
- **cold start が一回の Vertex AI 呼び出しぶん伸びる。**

## 6. やらないこと

- **PDF のテキスト抽出・OCR。** 解釈はクライアントのエージェントの仕事で
  ある(§3)。
- **どのファイルが当たったかの帰属表示。** 既存の検索も字句とベクトルの
  どちらで当たったかを出していない。
- **ファイル本文の DB への複製。** バイト列は GCS のみで、Postgres に本文の
  写しを持ち込む二重管理はしない。
- **許可する形式の拡張。** 許可リストは「Claude が読めて Gemini が埋め込める」
  交わりのままである。**読めないものは貯めない。**
- **モデル切り替え時の自動再インデックス**、および**次元変更後の自動
  reembed**(§2)。支払いは頼まれてから行う。
- **`references/` という慣習を ochakai が解釈すること。** SPEC §6 が名付ける
  置き場所は往復で保たれる**パス**であって、意味ではない。ここを解釈し始める
  ことは、ochakai がバンドルの中身を読んで判断する製品になることである。
- **幅ごとにベクトルのテーブルを残すこと。** 誰も読まないテーブルに行が残る
  ことは purge —「消したものは消える」— と両立しない。
