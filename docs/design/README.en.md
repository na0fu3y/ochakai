# Design records, in English

The decision records in this directory are Japanese, immutable, and
authoritative. This file is neither immutable nor authoritative: it is a
reading aid for people who cannot read them, so that the citations to
"design doc NNNN" scattered through the README, the architecture doc and
`api/openapi.yaml` lead somewhere. Where a row here and its record
disagree, **the record is right** — say so in an issue and this file
gets fixed.

One row per record: the record it links to, its status, and the decision
in a sentence or two. The grouping follows [the index](README.md) — start
with the table at the top of it for which record still describes an area
today. Numbers have gaps: proposals that never landed stay in their pull
requests.

A record that has been superseded gets a one-line pointer to whatever
replaced it instead of a summary — that is enough to follow the trail and
is all the maintenance it earns (design record
[0048](0048-decision-records-for-wire-contracts.md) §2.5).
`TestEnglishDesignIndexCoversEveryRecord` holds every record to at least a
row, and `TestEnglishIndexSummarizesOnlyWhatIsCurrent` holds a superseded
one to the pointer and nothing more.

For the shape of the system rather than the history of it, read
[docs/architecture.md](../architecture.md) (Japanese) first.

## Architecture and foundations

| Record | Status | Decision |
|---|---|---|
| [0081 What ochakai is, and what it refuses to hold](0081-what-ochakai-is-and-what-it-refuses-to-hold.md) | Accepted; the current record for the overall architecture (carries 0001) | A context provider for data agents that runs no LLM and executes no SQL — interpretation stays the calling agent's job, and human-verified knowledge is returned as it was written. One Go binary serving MCP and REST on one port, with **PostgreSQL as the only runtime dependency**: no Redis, no separate vector database, no search cluster, because pgvector works on plain Postgres and Cloud SQL alike. Distribution is a multi-arch GHCR image built `CGO_ENABLED=0 -trimpath` onto distroless static, with SBOM, SLSA provenance, keyless signing and SHA-pinned Actions. A rejection is kept as a ledger rather than a status — the memory of no that stops an agent re-proposing what a human turned down, opt-in to retrieve and excluded from search by default. §5 maps each of 0001's sections to whichever record owns it now. |
| [0001 Overall architecture](0001-architecture.md) | **Superseded by 0081** | |
| [0003 Google Cloud only](0003-gcp-only.md) | Accepted; Vertex AI became a default-path dependency in 0053 | Cloud Run + Cloud SQL (optionally Vertex AI) and nothing else. Portability is promised for your data (OKF export), not for the runtime — there is no supported deployment elsewhere. |
| [0115 The second footing waits for search](0115-the-second-footing-waits-for-search.md) | Accepted; amends [0003](0003-gcp-only.md) | **0003's "no supported deployment elsewhere" is not loosened by a line; what this record adds is the condition under which it may be withdrawn.** 0086 amended 0003 for authentication and stopped there, saying so itself — and no record said what lies past the stop, which leaves a reader unable to tell a permanent decision from one waiting for a day that has not come. Three things stay bound to Google Cloud after 0086, and **they are not the same weight**: the database credential and file bytes can be handed back to the operator, because saying what is lost turns each into a choice that falsifies none of the eight conditions. Embeddings cannot: **C8 rests on search**, 0080 §1 raised embeddings to the default because they are what works best here, and off Google Cloud they are absent rather than merely off. Nor does it look broken — [0114](0114-a-degraded-ranking-says-so.md) §2 already settled that a deployment with no embedder is not *degraded*, since lexical is its whole answer every time, so no flag stands. Not broken: **a different product**. Hence the condition, and the reason for it — **declaring support for a footing not worth supporting adds one lie**, and C4's "you can deploy it yourself" buys nothing if what you deployed fails C8. Written so it can be measured rather than felt, all three true: the embedder turns on with no setting added (0080 §1.1's metadata-server discovery has an equivalent off Google Cloud), it introduces no secret (an API-key provider is disqualified — it breaks the very property C2 names), and the operator still chooses where the text is processed (0080 §1.2). **No mechanism is chosen here**; nothing satisfies all three today, which is what makes the condition unmet. This record is not the amendment either: on the day the condition holds, a new number amends 0003, and the order cannot be reversed. Declined: an API-key embedding provider, adding a second storage engine before the condition is met (a single-file database removes the credential and does nothing for search, so it doubles the store for a footing not yet worth supporting), the phrase "multi-cloud" (0081 §2 counts what it takes to run, not where), and staying silent — a decision nobody can distinguish from a permanent one does not work as a ruler. |
| [0119 The unit of an operated fleet is a deployment or a directory](0119-an-operated-fleet-is-deployments-or-directories.md) | Accepted | **No decision moves; what this record writes down ahead of time is the shape in which one operator can run ochakai for dozens of organizations without it becoming a different product.** The unit of a boundary is one of two things that already exist: an organization that needs isolation gets a **deployment** — one Cloud Run service and one database, in its own project or as another database inside a shared Cloud SQL instance — and an organization that accepts one failure domain (region, embedding model, recovery unit shared with others) gets a **directory** inside a shared deployment, bounded by [0109](0109-a-directory-has-readers-and-writers.md)'s grants. The two are on the same road: a directory is an address ([0075](0075-the-bundle-is-the-address-space.md)), what is exported from under it is an ordinary OKF bundle, and a directory organization graduates to a deployment by export and import with no transformation. The operator is a set of IAM principals, never a holder of keys: taking an organization on adds bindings, handing it back removes them, and no secret changes hands (C2). The knowledge sits in the organization's own project by default, so C1, billing and data residency stay theirs; an operator's restore from a bundle lands every concept under the operator's name and `unverified`, which is why Cloud SQL backups are the operational recovery and export is the exit. Upgrading is changing `image_tag` and applying, with startup migrations additive and serialized under an advisory lock. **Four existing decisions hold the shape open, and a change that breaks any of them is the change that closes it**: boundaries are drawn by address ([0065](0065-identity-and-provenance.md) §1, 0109, 0075 §7), secret-zero ([0003](0003-gcp-only.md)), the bundle leaves and returns whole, and ochakai phones nobody. Declined on that basis: a tenant column (a second scope beside the address, which 0109 §7 and 0075 §7 already refuse as the thing that drifts; it buys nothing — isolation is the same single enforcement point a directory has, and per-tenant configuration is what a deployment gives for free), operator-facing telemetry or a control plane, fleet tooling and a signup surface in this repository (ochakai holds no user database; registration is the issuer's job and placing a grant is the operator's automation), and promising a "managed edition" anywhere — the FAQ's "there is no hosted version" stays true. **The price today is the operator's**: identities are Google's (Cloud Run IAM; [0086](0086-a-second-way-to-say-who-is-calling.md)'s OIDC was written for off Google Cloud, and a publicly invokable Cloud Run deployment verifying tokens itself is a posture nobody has spelled — the homework [0116](0116-the-connector-price-changed-not-its-condition.md) named and PR #721 ran on a public URL — while the CLI and the stdio bridge mint Google tokens only), the compatibility window (latest release only, one release per retired name per [0088](0088-a-retired-name-answers-for-one-release.md), CLI upgraded with the server; policy, not mechanism, and not moved here), shared-instance economics (PITR is per instance, connections scale 4×), and, for the directory form, what 0109 §3 pooled on the admin — `stats`, export and misses are whole-bundle, so a directory organization cannot take its own share; `PUT /api/v1/access` replaces the whole document with no `If-Match`; there is no rate limit ([0066](0066-four-postures-one-word.md) §7) and one Vertex AI bill. **The product may move, in this order**: the public-invoke self-verifying posture first (nothing self-serve exists without it), then the splits 0109 §3 itself foresaw (scoped `stats`, export and misses, `If-Match` on the access policy in [0030](0030-optimistic-locking.md)'s shape, a principal that may place rules only under its own prefix — each touches the wire, each gets its own record), then rate limiting, which waits for somebody to measure against. Like 0115 this record implements none of them. Declined: saying nothing, a ROADMAP line alone, a "running for many organizations" section in the operating guide (a runbook for a service that does not exist), and calling the directory form "multi-tenancy" — that word names the tenant column, and the product has 0109's grants. No surface moves. |
| [0053 Embeddings are the default](0053-embeddings-by-default.md) | **Superseded by 0080** (via 0073) | |

## Quality gates

| Record | Status | Decision |
|---|---|---|
| [0035 Machine-checked invariants](0035-verifiability.md) | Accepted | Invariants Go's type system can't carry are checked from outside: golangci-lint (with `exhaustive`), an OpenAPI contract test on every REST integration request/response, and fuzzing plus round-trip tests on the OKF parser — a linter is admitted only if a clean tree reports nothing. No user-facing effect; it is why an endpoint drifting from the published spec fails CI. |

## How decisions get written down

| Record | Status | Decision |
|---|---|---|
| [0048 Decision records are for wire-contract decisions](0048-decision-records-for-wire-contracts.md) | Accepted; §2.1 / §2.2 amended by [0128](0128-a-number-is-for-an-area-not-a-rule-inside-it.md) | Narrows what earns a numbered record to decisions a user can observe (the wire, a stored form, identity/provenance, a new Google Cloud dependency, a refusal); internal changes belong in a PR instead. An unreleased record is revised by replacing it, never by taking a new number. The 47 existing records stay untouched; the index gains a table of which record to read per area, and full summaries here are owed only to records that are still current. |
| [0128 A number is for an area, not a rule inside it](0128-a-number-is-for-an-area-not-a-rule-inside-it.md) | Accepted; amends [0048](0048-decision-records-for-wire-contracts.md) §2.1 / §2.2 | **Adds grain to 0048's criterion.** Observability picks which areas get a record, not how many records one area is cut into: 0109 took five amendments in a week and the index's authorization row came to list nine records. A rule *inside* an area's decision — who may write the first policy row, what a scoped caller's counts include — takes no number; it goes in the PR and the CHANGELOG. An unreleased parent is replaced (0048 §2.3, unchanged); a released parent that now contradicts its rules is amended by **one record that restates the area's whole current picture** and supersedes the parent and every amendment on it — the `design-doc` skill's step 7, made a rule because nothing checked it. CONTRIBUTING declares `INDEX-ROW-RECORDS`, the most records one row of the index's opening table may cite, and a test reads it back; it only moves down — from today's 10, one fold per PR, widest row first, to 3. Declined: dated appendices to released records (a citation would then point at different text per tag), periodic folding without changing the rule (done on 08-02, undone in 16 days), releasing less often (a decision about users, not about records — compatible if it happens), and splitting the table's rows (the grain problem replayed in the table). |

## Authentication, authorization and provenance

| Record | Status | Decision |
|---|---|---|
| [0065 No authorization; only who a write is recorded as](0065-identity-and-provenance.md) | Accepted; the current record for identity and provenance (carries 0002, 0027, 0032, 0052); §1 amended by [0109](0109-a-directory-has-readers-and-writers.md) | No authorization by default — whoever can reach a deployment can read and write, and headers are read only to record provenance, so trust is judged by the reader from it. The actor comes from the ID token Cloud Run's IAM already verified (`human:` or `process:`). A caller listed in `OCHAKAI_DELEGATING_CALLERS` may name an end user with `Ochakai-On-Behalf-Of`, recorded by composition (`human:x via process:sa`) and never by replacement, since a delegation you cannot tell apart from a direct write is indistinguishable from impersonation; an unlisted caller's header is a 403 rather than a silent downgrade. A caller's self-declared build (`Ochakai-Producer`, MCP `clientInfo`, `OCHAKAI_PRODUCER`) goes in a fourth actor field *beside* the authenticated name, which is the condition on admitting a self-declaration at all. Both web UI proxies always strip a browser-supplied delegation header, and `serve-ui` rebuilds one only from an IAP assertion it verified. Consolidates four records; no decision changes. |
| [0066 Four postures, one word](0066-four-postures-one-word.md) | Accepted; the current record for the deployment posture (carries 0040, 0042, 0060) | Two axes — is the caller identified, can anyone write — give four mutually exclusive postures, spelled with one `OCHAKAI_MODE` (unset / `read-only` / `public` / `dev`) rather than three booleans that could write eight. `read-only` refuses every write at a single point in the service layer regardless of caller, with usage telemetry deliberately outside the freeze; `public` reads no identity at all (every caller is `human:anonymous`, nobody gets a 401) and implies read-only, which is what makes a deployment anyone can reach exposable without IAM in front — an unsigned token forging any identity is the reason reading none is safer than reading unverified ones. An unrecognized value is a startup error, not a guess. Consolidates three records; no decision changes. |
| [0002 Authentication and authorization](0002-authn-authz.md) | **Superseded by 0065** | |
| [0040 Deployment-wide read-only mode](0040-read-only-mode.md) | **Superseded by 0066** | |
| [0042 The public read-only posture](0042-public-read-only.md) | **Superseded by 0066** | |
| [0060 One word for the posture](0060-one-word-for-the-posture.md) | **Superseded by 0066** | |
| [0027 Delegated end-user provenance](0027-delegated-provenance.md) | **Superseded by 0065** | |
| [0052 The self-declaration goes beside the actor](0052-producer-beside-the-actor.md) | **Superseded by 0065** | |
| [0009 OKF/Git round-trips and who owns provenance](0009-provenance-portability.md) | **Accepted** | Provenance is what an instance observed, not a portable attribute: bundles carry knowledge only, and a document's own trust family never reaches a ledger — 0075 §3.1 holds the current statement of that. What stays here is the two refusals it implies (`--preserve-provenance`, and signed provenance between instances — nothing authorizes either without an authorization model), and the ruling that Git is a review route: a round trip into the same instance moves no provenance at all, whoever imports is recorded as the actor, and **a merge is not a verification**. The procedure is [the Git-review guide](../guides/git-review.md) (Japanese). |

## The knowledge model — structure, ids, types, names, links

| Record | Status | Decision |
|---|---|---|
| [0079 Taking the document](0079-taking-the-document.md) | Accepted; the current record for what an import refuses and what the CLI sends; amends [0075](0075-the-bundle-is-the-address-space.md) §§3, 4.2 and [0074](0074-the-document-and-the-vocabulary-that-asks-it.md) §1. **Changes no REST behaviour** | A consumer's job under SPEC §11 is to take the document, and every place ochakai refused one was a place a user's bytes could vanish. **Two of the refusals were ochakai's own rules mistaken for the spec's**: `executor.receipt` was demanded and the untyped §4.1 keys (`title`, `description`, `status`, …) were forced to strings, both citing SPEC §10.2 for requirements §10.2 does not state. A non-string scalar is now read as the text it was written as, with a note; `tags` members are stringified; only unparseable frontmatter and a missing `type` — SPEC §11's own conditions — still refuse a document. A hand-written `index.md`/`log.md` is still regenerated rather than stored, but says so instead of vanishing (which makes `export \| import --strict` of ochakai's own bundle fail — deliberately). `ochakai put` and `ochakai import` send the document's own bytes instead of re-rendering the canonical form from the parsed fields, so a CLI write and a REST write of the same file store the same thing. One finding is **deliberately not fixed**: a document the write path refuses is reported rather than stored, because keeping its bytes would have needed a new rule on `PUT /api/v1/bundle/{path}`, and that wire freezes permanently under [0064](0064-rest-stops-at-api-v1.md) — the source bundle still holds the document and the skip says so. What is fixed is the refusal spreading: files the refused concept's body pointed at are written anyway, attribution being derived rather than stored. |
| [0075 The bundle is the address space](0075-the-bundle-is-the-address-space.md) | Accepted; the current record for the bundle, addressing and the stored form (carries 0017, 0021, 0041, 0046) | ochakai holds one bundle — a path→object map with two kinds of object, concepts (`.md` with a `type`) and files — and **whatever went into the bundle comes out**: a `.md` without a type is kept as a file rather than dropped. The path is the address and the type is an attribute, so layout is the user's (no type is inferred from a path, and a tarball's shape is the structure). What is stored is the bytes received; the canonical form is derived and used only for indexing and for deciding whether the meaning changed, so a reformat moves the ETag but not `generated.at`. Server-owned keys are stripped line-wise and another instance's trust family survives as a claim under `received:`, never entering a ledger. Attribution is derived from the body, `move` carries history, usage and the `<id>/` namespace in one transaction, and `prefix` narrows by address at segment boundaries — explicitly not an access boundary. Consolidates four records; no decision changes. |
| [0074 The document, and the vocabulary that asks it](0074-the-document-and-the-vocabulary-that-asks-it.md) | Accepted; the current record for a concept document's shape and the filter vocabulary (carries 0019, 0022, 0024, 0047, 0061) | `title` is optional (display falls back to the id's last segment) and the id joins the search haystack; only strings compared as keys are NFC-normalized, never content. Links come from markdown in the body — the structured `rel` was a machine-readable type no machine ever read — and `move` rewrites bodies too. Id segments are bounded by path safety alone. `fm.` answers only the keys OKF defines and refuses the five that have typed columns, so a caller can always tell an empty result from a misspelled key. A `dry_run` on the write path returns the real verdict without writing, because half of what `--strict` decides lives on the server. Consolidates five records; no decision changes. |
| [0071 The recommended type vocabulary](0071-the-recommended-type-vocabulary.md) | Accepted; the current record for the type vocabulary (carries 0038, 0063) | Nine recommended types, and the set is never closed — an unlisted type is first-class and nothing migrates. Three things decide the list: whether the spelling is self-explanatory on its own, whether OKF gave it a spelling, and whether anyone actually wrote it. The third is what removed `Playbook` and `API Endpoint`: neither appears in ochakai's own examples or in OKF's reference bundles. The vocabulary is stated in one place and checked from outside. Consolidates two records; no decision changes. |
| [0047 The filter vocabulary is the keys OKF defines](0047-fm-carries-okf-keys.md) | **Superseded by 0074** | |
| [0061 A dry run is the write withheld](0061-a-dry-run-is-the-write-withheld.md) | **Superseded by 0074** | |
| [0046 The bundle is the address space](0046-bundle-address-space.md) | **Superseded by 0075** | |
| [0043 The document is the truth](0043-document-first.md) | **Superseded by 0046, then 0075** | |
| [0041 Narrowing a search by address](0041-path-scoped-search.md) | **Superseded by 0075** | |
| [0037 Making declared expiry and cited sources queryable](0037-stale-and-source-lookup.md) | **Superseded by 0069** | |
| [0036 OKF's schema is ochakai's schema](0036-okf-schema-first.md) | **Superseded by 0043, then 0046, then 0075** | |
| 0034 OKF v0.2 conformance | **Withdrawn — number vacant** | Folded into 0036 and the file deleted before any release carried it; only the document was withdrawn — the implementation shipped. |
| [0005 OKF compatibility and knowledge structure](0005-okf-compatibility.md) | **Superseded by 0036, then 0075** | |
| [0016 Alignment with the knowledge-catalog reference bundles](0016-knowledge-catalog-alignment.md) | **Superseded by 0036, then 0075** | |
| [0017 The path is the address; the type is an attribute](0017-path-addressing.md) | **Superseded by 0075** | |
| [0019 Pre-0.10.0 consistency adjustments](0019-release-review-adjustments.md) | **Superseded by 0074** | |
| [0022 The filename is the name](0022-filename-as-name.md) | **Superseded by 0074** | |
| [0023 One type vocabulary, OKF's](0023-okf-type-vocabulary.md) | **Superseded by 0038, then 0071** | |
| [0038 Realigning the recommended vocabulary](0038-type-vocabulary-realignment.md) | **Superseded by 0071** | |
| [0063 Two unused recommended types leave](0063-two-unused-recommended-types-leave.md) | **Superseded by 0071** | |
| [0057 Concept is the word a reader meets too](0057-concept-is-the-word-a-reader-meets.md) | Accepted; **breaking (wording only)**; the current record for the unit of knowledge's name (carries 0054); §3.2's exclusion of the `entries` JSON key is revoked by [0064](0064-rest-stops-at-api-v1.md) §7, and §0's tool table is amended by [0076](0076-two-tools-leave-mcp.md) | Renames the five MCP tools carrying `knowledge` to OKF's word (`search_concepts`, `get_concept`, `put_concept`, `delete_concept`, `get_concept_usage`; tool count stayed 8 at the time — 0076 has since taken `delete_concept` and `get_concept_usage` off MCP, leaving 6, and 0064 §6 renamed `get_attachment` to `get_file`) and completes that by renaming every word a reader meets — README, docs, examples, CLI help/output, web UI labels, MCP/OpenAPI descriptions, error text — from `entry` to `concept`; identifiers (Go type names, DB columns, the Japanese records) are untouched. The `entries` JSON key was originally excluded too (issue #411 found the exclusion did not hold in two of the three places it applied; 0064 §7 renames it to `concepts`). |
| [0054 The unit of knowledge is called a concept](0054-concept-is-the-okf-word.md) | **Superseded by 0057** | |
| [0106 A read carries what points at it](0106-a-read-carries-what-points-at-it.md) | Accepted; amends [0067](0067-four-faces-and-what-they-decline.md) §5.1 | **A single-concept read now carries `linked_from`**: rows (id, type, title, description, status, trust) for the concepts whose bodies link at the one being read — address order, capped at 20, rejected linkers excluded — on the REST JSON, `get_concept` and `ochakai get`. Links are derived from bodies, so the forward direction is visible in the document itself; only the reverse lives in other people's documents, and a reader of a metric could not see the insight that explains it. The `links_to` filter is not folded: it stays the complete, pageable reverse lookup for callers who know to ask, while these rows are the head of the same answer for callers who did not — a pointer that has to be asked for is a pointer that does not get followed. Rows are pointers, not delivery: nothing is recorded as fetched, and the addition is response-only, outside the freeze (0082). The MCP schema does not move by a byte (0103 removed output schemas). |
| [0024 Links are derived from the body](0024-links-from-body.md) | **Superseded by 0074** | |

## Attachments

| Record | Status | Decision |
|---|---|---|
| [0080 What search fuses, and how a deployment embeds](0080-search-and-how-a-deployment-embeds.md) | Accepted; the current record for search and embeddings (carries 0020, 0053, 0073, 0078) | On Google Cloud the project **and the region** are discovered from the metadata server and semantic search is on by default; IAM decides whether it works, not configuration, and a discovered default degrades to lexical while a named one refuses to start. **Text is embedded in the region the deployment runs in** — a deployment in asia-northeast1 does not send concept bodies and search queries to another continent to be embedded, and where the region cannot be read ochakai embeds nowhere rather than picking a region nobody chose. No fixed default is right: us-central1 exports a Japanese deployment's text, and asia-northeast1 is the same mistake mirrored. One variable says how a deployment embeds: `OCHAKAI_EMBEDDINGS` takes unset/`on`, `off`, or a Vertex AI model resource name — the one string that already carries project, location and model together — and anything else is a startup error naming the three forms. The vector width is a per-model constant (768), not a setting, and a model ochakai knows no width for is refused rather than guessed at; a width change rebuilds the vector tables itself, because **vectors are derived, not records**, and refilling is `reembed`, paid for when asked. Search fuses three lists: lexical (filenames included), concept vectors and file vectors, with a file's hit mapped onto the concept that owns it. Files never come back on their own, and ochakai never interprets them. Consolidates four records; no decision changes, except that 0073 §6's claim of a write-side media-type allowlist is corrected — there is none, and the five embeddable types decide only what is worth an embedder call. |
| [0089 A half-embedded concept says so](0089-a-half-embedded-concept-says-so.md) | Accepted; amends [0080](0080-search-and-how-a-deployment-embeds.md) §3 and §7 | A concept longer than the embedding model's input window is embedded from its front and the rest is dropped — and nothing about the result looks wrong: it has a vector, it ranks, it comes back, and it is simply unfindable by anything said in its second half. The longer and more valuable the document, the more of it is gone. The flag lives on the vector row rather than on the concept, because the same concept is truncated under one model and whole under another; it is derived like the vector beside it, so existing rows read false ("not known to be truncated", which is what an un-reembedded row honestly is) until the next write or `ochakai reembed`. `GET /api/v1/stats` answers `embedding` with `enabled`, `vectors` and `truncated` — `enabled` because `vectors: 0` otherwise conflates a deployment without semantic search with one whose corpus is entirely unembedded, and the other two read as a pair: three in four is the shape of the corpus and wants a wider model, three in four hundred is one long runbook and wants splitting. CLI prints the pair (`-` with no embedder, as with misses); the web UI draws it only when it is not zero, as it does for dropped observations; MCP does not carry it. **Chunking is declined**: nobody can yet name who got stuck, because until now no surface said this was happening at all; the one-concept-one-vector assumption is load-bearing in the primary key, the search SQL, the reembed cursor and the stats count, and how chunks fold back into a concept is a ranking decision no record has made; and a document over the window is more often two concepts than one long one. §4 states the condition for reopening it. Files are not counted — a second table, a second number, and a file stays findable by name whatever happens to its bytes. |
| [0114 A degraded ranking says so](0114-a-degraded-ranking-says-so.md) | Accepted; decides what [0080](0080-search-and-how-a-deployment-embeds.md) §1 left open, and amends [0082](0082-what-the-freeze-holds-still.md) §3 | **A search whose query could not be embedded now answers with `degraded: true`.** The fallback itself is old and deliberate — losing the embedding provider degrades the fusion to the lexical list rather than failing the search — but the only place it was said was a `Warn` in the log, which an operator reads afterwards and the caller holding the thin result never sees. Same shape of silent failure [0089](0089-a-half-embedded-concept-says-so.md) §1 named for truncated vectors: the failure wears the face of a success. Who it hurts is decided by C8: **a trigram index cannot retrieve a two-character Japanese word**, so on a Japanese deployment losing embeddings means search returns close to nothing, and the user's first suspicion is their own phrasing — which no rewording can fix. **A deployment with no embedder is not degraded**: lexical is its whole answer every time, and a flag that stands on every response says nothing about any of them. One value on the wire, worded per surface: REST carries the key (absent, not false, when there is nothing to say), the CLI prints a `note:` line on **stderr** so the piped bytes are unchanged, the web UI says it in Japanese to the Japanese-speaking curator reading it, and MCP names the word in `search_concepts`'s **description** — this server declares no output schema, so a `jsonschema` tag would define the field for nobody. MCP's output type splits in two along the way: a search loses `cursor` (a search does not page) and a listing never gains `degraded` (it embeds nothing), each of which was a schema advertising a field the server could not write there. Resident bytes 11,629 → 11,788, `MCP-BYTES` unmoved; a boolean is not a word, so `VOCAB` stays at 49. The freeze check gained an arm in the same PR: [0082](0082-what-the-freeze-holds-still.md) §3 listed **two** places a response schema can live, and there are three — one written inline under an operation's `responses`, which is exactly where the search answer is spelled. It is response-only by construction like a `components/responses` entry, so the same rule applies; without the arm, a response-only addition in that one place would have had to claim a security defect. |
| [0091 A file vector is keyed by its path](0091-a-file-vector-is-keyed-by-its-path.md) | Accepted; amends [0080](0080-search-and-how-a-deployment-embeds.md) §5 | A file's vector was keyed by `(concept id, filename)` — the shape from when a file was something a concept *had*, addressable only at `<id>/<name>`. Once [0046](0046-bundle-address-space.md) and [0075](0075-the-bundle-is-the-address-space.md) made a file an object of the bundle attributed by any body that links it, the name became the path's last segment, and **a concept whose body showed `charts/q1.png` and `tables/q1.png` wrote both to one row**: the second vector overwrote the first and one of the two files silently left semantic search. Silently is the word — the file is still stored, served, exported and found by name; what is missing is only the vector half, which is the "ask it another way" path that embeddings exist for. The key is the path now, and **which concept a hit belongs to is read at search time from the bundle**, through the same attribution predicate the listings, backlinks and export already go through. Three things become true with it: a file the body links today ranks that concept today with nothing re-embedded, a file the body stops linking stops ranking it at once, and one file two concepts show ranks both from a single vector. Same reason, a file no concept names is embedded when it lands rather than waiting for a `reembed` to find an owner. The old rows are **dropped, not migrated**: a vector is derived and refilling is `reembed`'s job (0080 §3), and here the overwritten side no longer exists and a surviving row cannot say which file it described — guessing is worse than rebuilding. So an upgrade adds one step for the operator, `ochakai reembed`, and until it runs files are found by name, where a deployment without embeddings already is. The pending queue becomes a queue of files rather than (concept, file) pairs, holding only the media types this deployment's model can take: a zip in the old queue could never leave it, so `ochakai reembed` ended non-zero on every pass saying work was outstanding and pointing at a server log that said nothing. No surface changes. |
| [0105 A concept answers to its other names](0105-a-concept-answers-to-its-other-names.md) | Accepted; widens what [0080](0080-search-and-how-a-deployment-embeds.md)'s lexical index reads | **The lexical haystack now includes `synonyms`.** The type vocabulary describes a `Metric` as holding "what a number means and what it is called — its definition and **synonyms**", printed in the CLI help, the MCP schema and the architecture page; `examples/demo` writes them the obvious way (`synonyms: [net sales, top line, 売上]`); and `ochakai search 売上` returned nothing. The haystack was id, title, description, tags, body and the filenames in the concept's namespace, while a producer key sits in `attrs`, which nothing read — so the product invited a writer to record what their metric is called and then answered "no concept matched" when somebody searched for it. **Sharpest in Japanese**, where the warehouse column is `revenue` and the word in the room is `売上`, and `synonyms` is the natural place to tie the two together. It is appended where the trigger already appends the concept's filenames (migration 0040), reading either a list or the bare string somebody with one alias writes. **Only `synonyms`**: every other producer key is a value *about* the concept — a model, a receipt, an executor's resource, an id another concept owns — and indexing those makes a search for one concept return the concepts that merely mention it, which is the question `--links-to` and `--source` answer properly. The key stays the producer's: no validation, no wire field, no filter of its own, and [0043](0043-document-first.md) §3.6's "kept as written" is untouched — reading a key is not owning it. No surface changes; search results do, which is the point, and the search-evaluation harness holds the numbers. |
| [0073 What search fuses, and when embeddings apply](0073-search-and-when-embeddings-apply.md) | **Superseded by 0080** | |
| [0078 One variable says how a deployment embeds](0078-one-variable-says-how-it-embeds.md) | **Superseded by 0080** | |
| [0008 Image attachments](0008-image-attachments.md) | **Superseded by 0046, then 0075** | |
| [0011 Moving attachment bytes to GCS](0011-gcs-attachment-storage.md) | **Superseded by 0046, then 0075** | |
| [0013 Any file type, GCS only](0013-attachment-files-gcs-only.md) | **Superseded by 0046, then 0075** | |
| [0020 Attachment search](0020-attachment-search.md) | **Superseded by 0080** (via 0073) | |

The wire spelling `attachments`/`Attachment` outlived the concept 0046 §2.1
already retired. [0064](0064-rest-stops-at-api-v1.md) §6 is the record
that retires the name itself (`files`/`File` on the wire, `get_file` on
MCP) — it revises none of the records above; the storage model stays
0046's.

## Browse and the web UI

| Record | Status | Decision |
|---|---|---|
| [0072 The web UI: two serving paths, and editing documents](0072-the-web-ui-serves-and-edits-documents.md) | Accepted; the current record for the web UI (carries 0006, 0044) | One self-contained page that knows nothing about authentication, served two ways: `ochakai ui` binds loopback and acts as you, `ochakai serve-ui` is the deployed team service, and `ochakai serve` never serves the UI. Splitting them into two commands is what makes the dangerous combinations unrepresentable. Editing means editing the canonical OKF document as text — a form would need a YAML parser in the browser, and a thicker read shape would teach a form the format does not have. Losing the row editors is a stated cost. Consolidates two records; no decision changes. |
| [0092 The page is modules, so it can be tested](0092-the-page-is-modules-so-it-can-be-tested.md) | Accepted; amends [0072](0072-the-web-ui-serves-and-edits-documents.md) §1 | "One self-contained file" was how 0072 said "no build step", and that part does not change: no build, no framework, no CDN, and the page still knows nothing about authentication — what the browser fetches is what was written, with one `<script type="module">` instead of one `<script>`. What changed is that 2,438 lines shared a single namespace, and a namespace with no boundaries is a page no test can hold: to assert what a function returns you have to be able to import it. The first thing the split found was **four functions declared and never called** — invisible until then because there was no way to look. The modules are cut by direction of dependency rather than by feature, so five of them (escaping, formatting, the markdown renderer, the vocabularies, the document edits) never touch `document` and Node can import them, which is what makes the tests runnable. **No bundler, no framework, no npm dependency**: bundling would make what ships differ from what was written, which is the thing "no build step" was avoiding, and a `package.json` with a lockfile and `node_modules` is a supply chain a Go repository would then carry — the test runner ships with Node (`node --test`), and the browser smoke drives the Chrome DevTools Protocol over Node's own `WebSocket` in one file. One page became twenty assets, so they now carry **one ETag for the set** (the hash of every embedded file) with `Cache-Control: no-cache`: an embedded file has no modification time, so without a validator every load re-downloads everything, and a `max-age` would serve a cached module against an API that has moved on. The tests go from "13 greps over an HTML string" to three layers, with Go keeping only what a module cannot say about itself — the type vocabulary it shares with `internal/domain`, and the page-wide claims. CSP, a11y and Japanese are **made possible and not decided**. |
| [0094 The page runs under a policy](0094-the-page-runs-under-a-policy.md) | Accepted; decides what [0092](0092-the-page-is-modules-so-it-can-be-tested.md) §5 listed as made possible | The web UI is served under a Content-Security-Policy, which became writable when the page stopped carrying an inline script: `script-src 'self'` with nothing inline is most of what CSP is for, and a single inline script forecloses it. Measured rather than assumed — tightening the policy produces violations for inline **style** only, and none for script, which is the browser saying the script half is already satisfied. **`frame-ancestors 'none'` is a refusal a user can observe**: the web UI is the curation surface and the only place a human ruling happens, so a page that could be framed is one where whoever framed it knows where the verify button is and can put anything over it — somebody who wants to embed has REST, which is the contract the other three faces are clients of. `img-src` allows `https:` because a body may legitimately show a picture that lives elsewhere, and blanking those while shipping a security header would be breaking documents to look secure; `data:` and `http:` are not allowed. **`style-src` keeps `'unsafe-inline'`, and that is a cost stated rather than a gap hidden**: about forty literal style attributes would become thirty utility classes, while no attacker-controlled value reaches a style attribute (the one computed style, a table's alignment, is one of four constants) — the strict half is the half that matters and the loose half is named so tightening it later is tidying, not design. No `Referrer-Policy`: it would be a fourteenth counted header to improve on a browser default that already sends only the origin cross-site, and this page's own path is `/` because a route lives in the fragment. Accessibility is four gaps rather than an audit — the toast becomes a live region, focus follows a navigation into the main region, the current tab carries `aria-current`, and error banners are alerts. |
| [0126 A browser says where it came from](0126-a-browser-says-where-it-came-from.md) | Accepted; amends [0072](0072-the-web-ui-serves-and-edits-documents.md) §§1.1, 1.2 | **A non-safe cross-site request is refused by both serving paths, not one.** `ochakai ui` had refused cross-origin writes since it was written and `ochakai serve-ui` had no such check, while 0072 §1.2 said the two modes invert in "whether a browser guard is needed" — what actually inverts is the bind address and where identity comes from, not whether a write arriving from a browser can be trusted. The side without the check is the side with the conditions: behind IAP the browser holds a credential it did not have to be given (0065 §5), and `readJSON` does not look at `Content-Type`, so a `text/plain` form carries JSON to four POSTs with no preflight to stop it (a ruling, an outcome, a move, a reembed) — CORS headers ochakai does not send block reads, not writes. **Whether it is reachable is not claimed**: that turns on whether the IAP session cookie rides a cross-site POST, which is Google's attribute and was not tested; what is repaired is an asymmetry with no explanation. The mechanism is `net/http.CrossOriginProtection` (Go 1.25), and **what it does not do is what is being bought**: a request carrying neither `Sec-Fetch-Site` nor `Origin` is allowed, so the CLI, an agent over MCP and curl change by nothing, no counted surface moves, and there is no token to hold — reads (GET) are still served, since refusing them would take `/health` and the page itself down. **Not placed on `ochakai serve`** (§5): no configuration today lets a browser reach it holding a credential — IAM has no cookie, the OIDC path reads `Authorization`, `dev` is local, `public` is read-only, `sandbox` is anonymous — and a check that cannot fire is what 0035 §3.1 declines. The rebinding guard stays: a page at attacker.example resolved to 127.0.0.1 sends an `Origin` and a `Host` that agree, so **it is the one attack the cross-origin check is blind to by construction**, and the Host is what still gives it away. Folded away: the hand-rolled `Origin` comparison. |
| [0006 Two serving paths for the web UI](0006-web-ui-serving.md) | **Superseded by 0072** | |
| [0014 Folder browse](0014-folder-browse.md) | **Superseded by 0046, then 0075** | |
| [0021 Move, and web UI refinements](0021-move-and-webui-refinements.md) | **Superseded by 0075** | |
| [0044 The web UI edits documents](0044-web-ui-edits-documents.md) | **Superseded by 0072** | |
| [0032 Recording web UI writes under the IAP identity](0032-webui-iap-identity.md) | **Superseded by 0065** | |

## Surfaces — REST, MCP, CLI, web UI

| Record | Status | Decision |
|---|---|---|
| [0067 Four faces, and what each declines](0067-four-faces-and-what-they-decline.md) | Accepted; the current record for how the surfaces divide the work (carries 0004, 0007, 0015, 0033, 0039) | REST is the only contract and the other three are its clients; MCP's tool count is a context budget so its default is no; the CLI is the completeness surface, where completeness means capabilities rather than one command per REST operation; the web UI is a curation surface, not a BI tool. The CLI is a pure REST client (only `serve` touches the database, resolution is `--url` > `OCHAKAI_URL` > `ochakai use`, and there is no `ochakai login`), and `ochakai mcp-stdio` is a transport for the existing MCP face rather than a new one — it forwards JSON-RPC message by message so it never holds a second copy of the tool definitions. `/context`'s `hits` carry only a ranking on every surface, outside the byte budget. Also carries the per-surface list of deliberate omissions — rewritten in the vocabulary that exists today, since the old list still taught `search_knowledge`, `get_attachment` and `POST /api/v1/knowledge`. Consolidates five records; no decision changes. |
| [0068 How a face is added, and how one is removed](0068-how-a-face-is-added-and-removed.md) | Accepted; the current record for the rules that add and retire a face (carries 0050, 0056, 0058, 0062) | Four rules. **One capability, one command** — a second spelling buys convenience, not capability, and only the second one rots (`ochakai backlinks` and `ochakai queues` were folded away). **Two capabilities, two commands** — its converse, which split `ochakai search --sort` into `ochakai list [feed]`. **A listing is not a search** — listings take an opaque keyset `cursor` and return no total count, while a ranking refuses a cursor with a 400 since relevance has no page two. **An entrance nobody arrives through comes down** — `min_score` was removed outright (scores are uncalibrated across modes) and `fm.` came off MCP alone, returning 14% of the tool schemas without changing the tool count. Also carries the ruling face: one `POST /api/v1/review/{id}` taking `ruling`, spelled `withdraw` on every surface. Consolidates four records; no decision changes. |
| [0118 A call carries everything it needs](0118-a-call-carries-everything-it-needs.md) | Accepted; amends [0067](0067-four-faces-and-what-they-decline.md) §3 and [0065](0065-identity-and-provenance.md) §4 | **`/mcp` becomes stateless, and answers at MCP protocol version 2026-07-28.** ochakai builds its MCP endpoint on the Go SDK, and that SDK serves the current protocol only from a stateless handler — a session-holding one caps every client at 2025-11-25. ochakai held sessions by default, so it shipped the newest SDK and refused the newest spec, and silently: version negotiation succeeds, the tools work, and a client that has moved is never told it was moved back. **The mode being given up was never used** — no roots, no sampling, no logging (all three deprecated at 2026-07-28), no server-initiated request; ochakai's state is the knowledge base, not the connection. What it buys: the current protocol; no idle sessions accumulating (the SDK's session timeout defaults to never); and **more than one instance**, since sessions lived in one instance's memory and `max_instance_count = 1` was a consequence rather than a choice. No `Mcp-Session-Id` is issued or read, and GET and DELETE are 405 with `Allow: POST`. **The cost, stated:** at 2025-11-25 a client names itself only in `initialize`, which a stateless deployment answers in a throwaway session, so an older client's `clientInfo` no longer reaches the producer field (measured: `claude-desktop/1.2` becomes empty). What falls is the *self-declaration* 0065 §4 calls a record and not an identity — `created_by` is untouched, the `Ochakai-Producer` header covers it, and a client that moves to 2026-07-28 gets it back automatically. No counted surface moves; one piece of client-facing protocol (`Mcp-Session-Id`) goes away. |
| [0076 Two tools leave MCP](0076-two-tools-leave-mcp.md) | Accepted; **breaking (MCP)**; amends [0067](0067-four-faces-and-what-they-decline.md) §5.1, §6 and §7 | `delete_concept` and `get_concept_usage` come off MCP, taking the tool count from eight to six — 0068 §3's "an entrance nobody arrives through comes down", applied to whole tools rather than to a parameter. Deleting knowledge is a **ruling**, and this surface does not carry even the reversible rulings (`POST /api/v1/review/{id}`'s verify and reject are not tools): an agent that cannot be trusted to reject a concept cannot be trusted to delete one, and the guard in 0067 §6 only ever protected concepts a human had already ruled on — an agent could still erase its own unread draft. Usage totals are the loop's **human** half (0069); what an agent needs in order to rely on a concept, its trust tier and `verified_at`, already rides on every search hit and every `get_concept`, and it keeps `report_outcome`, the edge of the loop it owns. **Capability is lost from this face only** — REST, the CLI (`ochakai delete`, `ochakai usage`) and the web UI keep both. No deprecation window: MCP is explicitly unstable at 0.x. |
| [0111 Weight for the eye, and only for an eye](0111-weight-for-the-eye-and-only-for-an-eye.md) | Accepted; follows [0110](0110-the-first-column-is-the-key-you-asked-for.md) on the line it left; leaves [0067](0067-four-faces-and-what-they-decline.md) §5.3 standing | **The CLI's human-facing lines get bold and dim — and nothing else.** After 0110 removed the score, every field on a hit line carried the same weight, so one row's description ran into the next row's address and a wrapped line read as two. The separator that says where a field ends is a tab, which is for a machine and invisible to an eye. One rule: **the address is bold** (the URI, and `browse`'s directory names, segments and file names — the field you copy into the next command), **the prose trailing the row is dim** (`— description` and the `· snippet` that says why it matched), **and everything between stays plain** — `status`, `type` and a feed's first column are values the reader compares down the column, and a column you compare must not be faded. **Escapes are emitted only when stdout is a character device**, so a pipe, a redirect, `--json` and a test buffer get the bytes they got before, which is why this is not the TUI 0067 §5.3 declines and why it needs no compatibility procedure (0110 was BREAKING; this changes no contract). The property the whole thing rests on — **strip the escapes and the line is byte-identical to today's** — is checked by a test. No hue: a terminal's palette belongs to its user, weight has no theme and no colour-vision variant, a terminal that ignores dim degrades to today's plain line, and 0094's rule (what is said only in a rendering is not said) is satisfied only because bold and dim carry no information of their own. Rejected: colouring `status` (a green `stable` reads as "safe", but trust is judged from provenance, not lifecycle), marking turned-down rows (0104 decided they are spelled like any other row, and a rendering must not overturn that), highlighting query terms (a client re-finding the words would underline nothing on a hit the embeddings matched), styling `get` (that is syntax highlighting, a second web UI inside the terminal) and `stats` (`key\tvalue` has no structure to style, and 0069 promises `cut -f2`), and a `--color=auto|always|never` flag (`always` has no use here yet; adding it moves FLAG 28 → 29). **ENV 14 → 15**: `NO_COLOR`, honoured on presence rather than value, is the existing cross-program spelling rather than an `OCHAKAI_COLOR` nobody else reads — it counts because the ENV section counts calls, not prefixes, the same reason `PORT` is there. |
| [0110 The first column is the key you asked for](0110-the-first-column-is-the-key-you-asked-for.md) | Accepted; **BREAKING** (CLI); applies [0068](0068-how-a-face-is-added-and-removed.md) §3 to the CLI's human-readable output | **The relevance score comes off `ochakai search`'s printed lines**, which become `uri, status, title — description`. 0068 §3 struck `min_score` from the wire because scores are uncalibrated across modes; the same number went on being printed as a first column, where the problem is worse — a lexical-only deployment scores a hit 0 to 3 (weighted fragment coverage), a hybrid one scores it on RRF's scale, which `rrfK = 60` caps at about 0.049, so **the same query prints 1.730 on one deployment and 0.033 on another and the line never says which**. A deployment whose query embedding fails degrades to lexical silently, so the column also changes scale mid-flight with only a server-side warning. `ochakai list --source` and `--links-to` lose the column too: a reverse lookup orders by address and its score was a constant `0.000`. **The feeds keep their first column** — a date or a count is the key the reader asked to be ordered by and compares against the row below. `--json` and the REST `score` are unchanged, so a caller that has calibrated one still has it. The web UI had already reached this conclusion independently: it never renders the raw score and uses the same 0.05 to tell lexical from hybrid before applying a weak-hit floor, and MCP's tool schema never mentions it — **the CLI was the only face showing a reader the raw number**. Rejected: printing the rank instead (the row order already carries it, and silently changing a column's meaning is worse than removing it), normalizing the scales (what cannot be calibrated is the meaning, not the range), and labelling the mode (`1.730 (lexical)` invites "is 1.730 good?", which is the question 0068 §3 said the product cannot answer). Breaks `cut -f2` for id (now `-f1`); `cut -f1` for the score has no replacement outside `--json`. No surface count moves. |
| [0109 A directory has readers and writers](0109-a-directory-has-readers-and-writers.md) | Accepted; §2 amended by [0120](0120-the-policy-is-replaced-only-as-it-was-read.md); §3 amended by [0122](0122-the-first-rule-is-an-administrators-to-write.md), [0123](0123-the-numbers-say-what-they-counted.md) [0124](0124-a-directory-can-have-its-own-administrator.md) and [0127](0127-an-archive-says-which-part-it-is.md); the current record for authorization; amends [0065](0065-identity-and-provenance.md) §1 | **ochakai gains authorization: grants of read and write under a directory prefix.** 0065 §1 named the condition for revising itself — "when somebody who may read but not write is needed" — and it arrived as both halves at once: knowledge some readers must not see, and curated knowledge some writers must not change, inside a bundle that has to stay one bundle. The existing answer, a second deployment, was unavailable because the teams share a glossary: split it and each copy lands `unverified`, since an import reads no provenance back (0075 §3.1), so the price of the boundary would have been the verification C7 measures. The table holds **grants only** — a deny rule would make "why can Tanaka not read this" a question about precedence rather than about a row being present — matched on segment boundaries by 0075 §6's existing rule, and principals are spelled as the ledger spells actors (0065 §2), so the name in a grant and the name in a revision are one comparison. **A deployment with no rules behaves exactly as it did before this record**; the first rule turns the boundary on for everyone at once. Administrators come from `OCHAKAI_ADMINS`, not from a rule: whoever may edit the policy may grant themselves anything, so that answer cannot live in the thing being edited, and a deployment holding rules with no administrator refuses to start. Whole-bundle operations — stats, the export tar, move, reembed, and the policy itself — go to administrators rather than being narrowed, because a partial stats gives the loop's numbers a second meaning and a quietly incomplete export is still called a backup. A read outside the scope is **404, not 403**: whether something exists at an address is the boundary itself. **One leak is stated rather than closed**: a readable concept whose prose names a hidden id leaks that id, because 0075 §3 forbids the server rewriting a stored document, and redaction would cost the round trip C1 rests on. Enforced in one place (`service`), like read-only. The CLI (`ochakai access`) and the web UI (`#/access`) both carry it as one whole document replaced in one go; MCP does not, because a tool schema is paid for out of an agent's context window. The web UI's tab appears only for a caller the server let read the policy, and its editor states that `serve-ui` without IAP folds every caller into one service account — so everyone who can reach that URL can edit the boundary. Not doing: deny rules, roles, group registries (an OIDC issuer can carry claims — 0086 — if it comes to that), per-concept or per-type grants, and a history of the policy. |
| [0127 An archive says which part it is](0127-an-archive-says-which-part-it-is.md) | Accepted; amends [0109](0109-a-directory-has-readers-and-writers.md) §3 | **The whole bundle stays the administrator's; a subtree archive belongs to whoever may read it.** The refusal it lifts was written against a danger of a different shape, and measuring said so. `ochakai import` is purely additive and never deletes, so "restoring an incomplete export loses the rest" does not happen — what is lost was lost when the original was. The real hazard is quieter and **was already reachable**: taking a subtree archive today returns a tar.gz with a root `index.md` and the filename `ochakai-okf.tar.gz`, **identical in shape to a whole-base backup**, so an administrator could already produce something indistinguishable from one. Refusing scoped callers had never prevented that. So the archive says which it is, in the two places its reader meets it: the root `index.md` carries `bundle_scope` in frontmatter for anything reading the bundle and a sentence naming what is missing for the person who unpacked it, and the download arrives as `ochakai-okf-<subtree>.tar.gz` — **the filename is the one that lands, because whoever restores it types a name before they open anything**. Deeper `index.md` files and whole-base archives are byte-for-byte what they were: what a subtree archive lacks is above them, not below. With the two distinguishable, refusing the part buys nothing — a subtree archive carries what its caller could already fetch one concept at a time, so the refusal withheld convenience rather than knowledge. The root is still an administrator's (403), because an archive of "everything this caller can see" is precisely the unnamed part 0109 §3 feared, and a subtree outside the caller's read scope is 404 like every other read of it. This returns what [0119](0119-an-operated-fleet-is-deployments-or-directories.md) §4 counted as a price: C1, the exit, no longer needs the operator for an organization living in a directory. No counted surface moves — `ochakai export --prefix` reuses a flag name three commands already carry, and `Accept: application/gzip` on a bundle path has always read it as a subtree. Declined: teaching `import` to read `bundle_scope` and warn (imports do not read reserved files back, and a hand-written bundle carrying the key would get a baffling warning — while the filename reaches the restorer *before* they type `import`), re-rooting the subtree (ids change, links out break unfixably, and the graduation path 0119 §1 promises depends on paths surviving), a separate command, handing a scoped caller "the whole of what they can see", and signing or manifesting the archive — completeness is a different problem from mistaken identity. |
| [0124 A directory can have its own administrator](0124-a-directory-can-have-its-own-administrator.md) | Accepted; amends [0109](0109-a-directory-has-readers-and-writers.md) §3 | **A grant gains a third power, `may_admin`: the rules *under this prefix* are this principal's to edit.** 0109 §3's floor is right about the whole document — whoever may edit a policy can grant themselves anything, so "who may edit it" is configuration — and too coarse for one subtree: handing a team its own directory meant handing it the power to delete every other team's boundary, and the automation that places one grant per organization ([0119](0119-an-operated-fleet-is-deployments-or-directories.md)) had to hold full administration permanently. `may_admin` implies `may_write`, which implies reading, and **is refused at the root**, where it would put "who may edit this policy" inside the policy — the revolving door 0109 §3 named; the refusal points at `OCHAKAI_ADMINS`. **The floor does not move**: only a full administrator can create or remove a prefix administrator, and a prefix administrator has no path outward. **The hard part is the whole-document replacement.** A prefix administrator reads only the rules they may edit — the rest name people and directories they cannot see, as a scoped read is a 404 — so the obvious client flow (`access --json > f; edit; access -f f`) would delete everyone else. It does not: **rules outside their subtrees survive the write**, because a document that could not carry them says nothing about them. The split and the precondition happen inside one transaction under the advisory lock, so [0120](0120-the-policy-is-replaced-only-as-it-was-read.md)'s window stays closed, and **the version covers what was read** — a rule they cannot see moving must not 412 them, since they could not act on it. A rule sent outside their prefixes is a **403, never a silent drop** ([0065](0065-identity-and-provenance.md) §3's rule against quiet downgrades). A rule *shallower* than their own is deliberately not theirs: editing the rule that contains you is how a subtree grows into the bundle. **What is stated rather than closed** (§4): the effective access to a directory can be wider than the rules its own administrator can see, because a shallower grant reaches it — the same honesty 0109 §4.1 applies to ids named in prose. Migration 0045 is additive and defaults false, so every policy written before it means exactly what it meant, and no counted surface moves — `may_admin` is a boolean on a schema, not an operation, a parameter, a header, a flag, a variable or a vocabulary word. Declined: opening export, `move` or `reembed` to prefix administrators (0109 §3 and [0123](0123-the-numbers-say-what-they-counted.md) §4's reasons still hold; `stats` already narrows), showing them the shallower rules, a history of grants, and spelling prefixes into `OCHAKAI_ADMINS` — configuration answers who the floor is, while which directory was handed to whom is a fact belonging to the policy, and putting it in configuration would make every new tenant a redeploy. |
| [0123 The numbers say what they counted](0123-the-numbers-say-what-they-counted.md) | Accepted; amends [0109](0109-a-directory-has-readers-and-writers.md) §3 | **`stats` leaves the set of whole-bundle operations pooled on the administrator; export, `move`, `reembed` and the policy itself stay there.** 0109 §3 refused to narrow these because a subset would be "a second meaning for numbers the loop is measured by" — **the reason was right and the scope of it too wide**: `stats` has taken `prefix` since 0041, so an administrator could already produce that subset. What made it a second meaning was not the subset existing but **the answer never saying which one it carried** — the same row of numbers, sometimes the base and sometimes a corner of it, indistinguishable. So the response now declares `scope`: the prefixes counted, absent when they were the whole bundle, and **an empty list meaning the caller can see none of it** — which is what says the zeros below are zeros for that reason rather than because nothing happened (an empty prefix list cannot say it, since in a filter it means the whole bundle). An administrator asking about one subtree gets the same declaration: the field is about the numbers, not about the caller. It is [0114](0114-a-degraded-ranking-says-so.md)'s shape reapplied — what narrowed says that it narrowed. **The two figures that cannot be scoped say so instead**: misses carry no concept id ([0069](0069-the-loop-and-what-measures-it.md) §5.1), so a scoped caller gets `withheld` rather than the whole instance's — showing them unnarrowed would tell one team what every other team searched for — and it is a separate word from `recording: false` because the remedy differs (ask an administrator, versus change a setting); `dropped` stays instance-wide, being a footnote on how far the numbers above can be trusted rather than anything about knowledge. **Why only `stats` moves**: export rests on the bundle's one invariant, that what goes in comes out ([0075](0075-the-bundle-is-the-address-space.md) §1), and a declaration does not fix a quietly incomplete archive because a disaster-recovery runbook does not read declarations; `move` rewrites inbound links, so narrowing it breaks the ones it skipped; `reembed` pays Vertex AI for the whole base. `stats` differs in being an answer that exists only to be read. Fixed in passing (§5): `Stats.Queues` was documented as unscoped by prefix while the store had already been scoping it. Response-only additions, so outside the freeze ([0082](0082-what-the-freeze-holds-still.md) §2); no counted surface moves. Declined: showing scoped callers a narrowed miss list (there is no id to narrow by, and "queries containing that prefix" counts something else under the same name), leaving the 403 in place, always sending `scope` (its absence is how "the whole bundle" is spelled, and a new field on every answer reads as something having narrowed), folding `withheld` into `recording`, and splitting export at the same time — one at a time is the shape 0109 §3's "this can be split later" describes. |
| [0120 The policy is replaced only as it was read](0120-the-policy-is-replaced-only-as-it-was-read.md) | Accepted; amends [0109](0109-a-directory-has-readers-and-writers.md) §2 | **`PUT /api/v1/access` takes an `If-Match` precondition: the replacement lands only while the policy still has the version that was read, and a stale one is a 412 that writes nothing.** The policy is one document read, edited and put back whole, so two administrators who each add a rule leave only the second one's rule — no 400, no 409, both documents valid, and the only signal is the person who was dropped saying they cannot read something. 0109 §2 foresaw exactly this and wrote that If-Match already solves it for a concept ([0030](0030-optimistic-locking.md)); what made it worth adding is [0119](0119-an-operated-fleet-is-deployments-or-directories.md), because once grants are placed by automation rather than by a person, "read, append, put" is the ordinary path and the organization that loses its rule sees a 404 — the failure surfaces as a boundary working too well, invisible from the side that caused it. Semantics are the concept's, unchanged: absent means last write wins, `*` is a 400, and `If-None-Match` is a 400 here because the policy is one document that always exists, so a create-only precondition has nothing to mean. **The version hashes the grants and nothing else** — including `granted_at` would let a write that changes no rule invalidate a precondition somebody holds, the shape `etagOf` already refuses — **the order is imposed in Go** rather than taken from the read, since a database collation decides the SQL order and two deployments could otherwise hash one policy two ways, and **an empty policy has a version like any other**, which is the whole point: the first grant on a deployment with none is the write two automated callers race for. The comparison happens inside the replacing transaction under an advisory lock, because a version compared outside it leaves the window it was meant to close, and an empty table has no rows to lock. The version travels in both the `ETag` header and the body's `version`, the way a concept's `summary.content_hash` does, so the document `ochakai access --json` prints carries everything a conditional write of it needs; a `version` in a request body is declared `readOnly` and ignored rather than becoming a second spelling of the precondition. **No counted surface moves** — HEADER and FLAG count names, and `If-Match` is already among them (`ochakai access --if-match`, the spelling `put` and `delete` use). One thing was fixed in passing (§6): `GET /api/v1/access` answered `{"rules": null}` on a deployment with no grants, which the contract declares a required array — every deployment starts in that state, and no integration test had read an empty policy over the wire, so the contract check had nothing to check. Declined: per-row operations (0109 §2's refusal stands, and the precondition would still be needed), reading the body version as the precondition, making `If-Match` mandatory, a policy history, and merging on conflict — merging a set of grants turns a deletion into an overwrite, and a document where the absent row decides the answer can only ever err towards widening the boundary. |
| [0122 The first rule is an administrator's to write](0122-the-first-rule-is-an-administrators-to-write.md) | Accepted; amends [0109](0109-a-directory-has-readers-and-writers.md) §3 | **Writing a rule into the access policy is an administrator's own operation, and a caller outside `OCHAKAI_ADMINS` is refused before anything is written rather than after.** `SetPolicy` checked the caller, wrote, and then re-read the policy through the same check. On a deployment with no grants every caller passes that check — 0109 §2's promise that no rules means no boundary — so a caller nobody had named could write the *first* rule: the rows were committed, the log said the policy had been replaced, and the caller was handed a **403 with no version and no body**, because the boundary it had just created now excluded it. A 403 is the code that says nothing was written, and automation re-sends what fails, replacing the policy again each time — the shape [0119](0119-an-operated-fleet-is-deployments-or-directories.md) makes ordinary. The floor is 0109 §3's own, read from the caller's side instead of the configuration's: from the moment the first rule lands the policy belongs to `OCHAKAI_ADMINS`, so placing that rule is theirs too. §2's promise is untouched, because it is a promise about *knowledge* and 0109 §5 already put the policy outside the bundle. **This is not a compatibility break**: every call now refused already ended in a 403, since the check on the way out could never pass; what changed is that the 403 tells the truth. A second, more general rule comes with it — **no operation puts a check that can refuse on the far side of its own write**, so the read-back no longer re-checks the caller and returns what is stored, version included ([0120](0120-the-policy-is-replaced-only-as-it-was-read.md) §3). Clearing the policy is unchanged, as is the 400 a deployment naming no administrators gets. The web UI's tab still shows for everyone while there are no grants — following 0109 §6 fully would mean the server answering "are you an administrator", a second spelling of the authorization answer — so the save names the reason instead. Declined: dropping only the check on the way out (which would leave a non-administrator drawing everyone's boundary and unable to undo it), accepting the lockout with a 200 (the caller would learn from a 404 later, 0120 §1's invisible failure), a 400 (the refusal is about who called, which is what `ErrForbidden` already means), and granting the first writer administrator rights automatically (deciding the editor inside the edited document, 0109 §3's revolving door). |
| [0117 A person recorded as a process says so](0117-a-person-recorded-as-a-process-says-so.md) | Accepted; amends [0086](0086-a-second-way-to-say-who-is-calling.md) §4 | **A verified token with no usable email still records the caller as `process:<sub>` — the process now says so once, instead of doing it silently.** 0086 §4 wrote that branch for machine tokens, which legitimately have that shape. What it did not have in view is that a *person's* token can arrive the same way: issuers commonly put `email` in the ID token rather than the access token, so a human who completed a browser authorization writes concepts attributed to a process, and nothing fails — no 401, no 403, a 200 and a wrong name in the ledger, discoverable weeks later by reading the trail. [0065](0065-identity-and-provenance.md) §3 already refused this shape at the delegation door ("a silent downgrade always costs more than an explicit failure"); this is the same thing one door over. **It is not a refusal**, because nothing here distinguishes a machine's token from a person's, and refusing would break the correct machine configuration. It is a single `WARN` per process, naming the issuer and the subject and no key material — once, because in a machine-only deployment the correct configuration would otherwise raise it on every request, and an alarm that always rings tells nobody anything. Nothing else moves: the recorded name, the trust tier, and every response are what 0086 §4 already decided, and no counted surface changes. |
| [0108 The context pack retires](0108-the-context-pack-retires.md) | Accepted; **BREAKING** (non-core REST, MCP, CLI); supersedes [0093](0093-the-budget-governs-the-whole-response.md), amends [0067](0067-four-faces-and-what-they-decline.md) §§1, 4, 5.1 and [0096](0096-a-listing-is-not-a-search-here-either.md) §3 | **REST's `/context`, the MCP tool `get_context` and the CLI's context command retire from every surface at once.** Unlike 0076's two tools this one had traffic — the bundled hook, the distributed CLAUDE.md and the manual all went through it — so the grounds are different: the pack's premise aged out. It was built for a world where round trips were expensive for agents, and it made the server decide what should be read (greedy packing, outline, excerpt, a two-direction link hop); an agent that iterates search → get on its own is having that judgment taken away from it, and in a product that runs no LLM, deciding what to read is the most LLM-shaped job the server had. What the pack bundled survives in primitives: the reverse hop arrives as `linked_from` rows on the concept itself (0106), and the write-back hint moves to `get_concept`'s answer, the point where knowledge is actually delivered. The demand signal gets more honest, not less — a deliberate fetch is recorded where a pushed pack was not chosen by anyone. The recall hook becomes a pointer injector over `ochakai search --json`. Breaking with no window (a removal, not a rename); the REST removal rides on 0107 having moved `/context` outside the frozen core, so the fingerprint does not move. Declined: a smaller pack under another name, `budget` on search, a 0088 window. Revisit trigger: an agent on a host with no shell and no MCP iteration actually stuck needing one HTTP call for everything to read — the starting point is the pack as 0093 left it, outside the core. |
| [0093 The budget governs the whole response](0093-the-budget-governs-the-whole-response.md) | **Superseded by 0108** | |
| [0098 The generator works; what was missing was the declaration](0098-the-generator-works-what-was-missing-was-the-declaration.md) | Accepted; amends [0064](0064-rest-stops-at-api-v1.md) §11 | 0064 recorded that *no generator produces a working client for this one address*, and that turned out to be untrue. `/api/v1/bundle/{path}` declared its `path` parameter on `get` only — `put` and `delete` did not — so **the contract said three different things about three operations at one address**, and every generator stopped there (`has 1 positional parameters, but spec has 0 declared`). It was never the file's size or the freeze. Declared once on the path item, which OpenAPI shares across the address, generation succeeds, the client compiles, and GET / PUT / DELETE against a slash-bearing id all work — the generator percent-encodes the slash and Go's `ServeMux` decodes it back. **The freeze is not broken**: no line disappeared and none changed; the two lines added say what a caller was already sending. Verified with one generator (oapi-codegen for Go), so the claim retired is the categorical one, not replaced with an equally broad opposite. Still not doing: re-expressing `{path}` properly in OpenAPI (the empty root and the slashes are genuinely inexpressible — that half of 0064 §11 stands), and putting a generator in CI, where a third-party tool over the network would turn a quiet day red. |
| [0099 A purge reaches the bytes](0099-a-purge-reaches-the-bytes.md) | Accepted; amends [0031](0031-purge.md) §3.2 | **Blob bytes nothing references any more are reclaimed, after a purge and after a file delete.** 0031 declined this, weighing the storage cost of orphaned blobs against the risk of deleting a live attachment; the thing to weigh was the promise. The first of the eight conditions says an asset is deleted on request, a purge runs only when someone asked exactly that, and what stayed behind was not capacity but an untrue sentence — the store's own comment told the operator to write the sweep themselves. 0031's first two steps still hold: blobs are content-addressed and shared, so no single delete can reclaim them as its own side effect. What changed is that the global question is asked **outside** the request — the purge commits, then a sweep deletes every blob row no object and no attachment references. 0031's estimate assumed that scan had to happen inside the purge. The risk it feared is closed rather than accepted: the query reads the rows themselves instead of a reference count that could drift, writers hold a shared advisory lock from upload to commit while the sweep takes it exclusive (so bytes uploaded for a row not yet committed are never swept), and bytes go before the row that names them, so an interrupted sweep leaves only an unreferenced row the next sweep reads. **No surface is added** — no endpoint and no command call the sweep, and a failure is a logged warning rather than a failed request, since the rows the caller asked to destroy are already gone. Declined: a reference count (the drift is exactly 0031's accident), sweeping on every overwrite (an exclusive-lock scan on the busiest path — so for orphans made by overwriting, "deleted" means "deleted eventually", stated rather than hidden), sweeping inside the purge transaction, and a scheduled sweep (a new Google Cloud dependency for housekeeping). Note for anyone who read the old record: v0.13.0's release notes said purge "does not reclaim attachment bytes" — true then, false now. |
| [0097 A write says what it did, in the body](0097-a-write-says-what-it-did-in-the-body.md) | Accepted; amends [0074](0074-the-document-and-the-vocabulary-that-asks-it.md) §5 | The one word a write turned out to be — `created`, `updated`, `unchanged` — now rides in the response body as `plan` (REST's body is the View; MCP reads it at `knowledge.plan`), and **the headers stay**. The issue asked for a *move* from header to body and only half of that is available: [0082](0082-what-the-freeze-holds-still.md) §2 lists header names inside the frozen address space, so removing `Ochakai-Plan` and `Ochakai-Unchanged` is 1.0's work, while adding a property to a response-only schema was never what the freeze held. The reason for the half that is available is W5's: `curl` does not print headers and `jq` drops them, and **MCP has no headers at all** — an agent that wrote something had no way to learn whether anything changed short of reading it back, on the writing half of the loop this product is about. The three words moved to `internal/domain` from the two private copies in `internal/restapi` and `internal/apiclient`, and **that alone took `VOCAB` from 46 to 49**: not new words — `Ochakai-Plan`'s enum is in the frozen contract and `ochakai import` prints all three — but words no counter could see, because the count reads `domain` and the values were not there. Same shape as [0083](0083-an-error-carries-a-code.md)'s error codes: prose is free to rewrite, a value a client branches on is a word somebody learns. Not on file writes (a real file write does not compare the bytes, so it cannot say `unchanged`, and a field present on one of two paths is worse than the header), not on `move` or `review` (neither is one of the three), and the CLI keeps reading the header — a dry run with no header is how a server that ignored `dry_run` and wrote anyway is detected, and the body cannot make that detector. |
| [0113 A reinterpretation says itself in the body](0113-a-reinterpretation-says-itself-in-the-body.md) | Accepted; decides what [0097](0097-a-write-says-what-it-did-in-the-body.md) §4 left to its own record | **A write's response body now carries `notes`** — one line per value the server accepted but read differently than it was written (an unrecognized `status`, an unparseable date, a trust family kept as the document's own claim). The strings are not new: they have been in the repeated `Ochakai-Note` header since the beginning, and "a reinterpretation is never silent" ([0075](0075-the-bundle-is-the-address-space.md) §3.1) is why they exist. What was new is where they were not arriving. `curl | jq` drops headers and a generated client does not model them, but the surface that mattered is the **web UI**: the one face where a human edits a document — the only person who can go fix the line — never saw a note, because the value was in a header the page did not read. The save toast says it now, on the same path that reads `plan` ([0097](0097-a-write-says-what-it-did-in-the-body.md)), held longer than the 2.2 seconds sized for "saved". **The header stays**: a header name is inside the freeze ([0082](0082-what-the-freeze-holds-still.md) §2), so removing it is 1.0's work — the same half-a-trade 0097 took for `Ochakai-Plan` — and it is also the only carrier a `text/markdown` response has, since a body that *is* the document has nowhere to put a note. **MCP does not move**: it already returns the same strings in the body, one level up (`notes` beside `knowledge` rather than `knowledge.notes`), and unifying the nesting would either break MCP to buy symmetry or bury the notes under the concept — when both directions are worse, an uneven spelling is not a debt. The CLI keeps reading the header, which works for both representations. No counted surface moves: `notes` is prose a person reads, not a value a client branches on, so `VOCAB` stays at 49 and `HEADER` at 13. |
| [0096 A listing is not a search here either](0096-a-listing-is-not-a-search-here-either.md) | Accepted; **breaking (MCP)**; amends [0068](0068-how-a-face-is-added-and-removed.md) §§1-2 | `list_concepts` splits off `search_concepts`, taking MCP from six tools to seven. **No capability is added.** What is split is a schema that could not state its own rules: `query` was required unless `sort` was set, `cursor` was refused unless it was, and `limit` meant 10/50 one way and 100/1000 the other — none of it expressible in JSON Schema, so all of it lived in prose the agent had to obey unaided, and an invalid combination cost a turn to discover. Sending `sort` to `search_concepts` is now refused by schema validation before a handler runs. 0068 §2 said the wire stays one operation because the filters are identical and so is the response, and this decides that MCP follows the command side instead: the resemblance to the wire is only in how the cost is paid, and what an agent holds is not understanding — a model re-reads the same prose every conversation and misreads it at the same rate, while a person who learned the rule has learned it. **The price is not hidden**: seven filters are duplicated, resident bytes go 11,829 → 13,433 (+13.6%), and `MCP-BYTES` rises 12,000 → 13,500 — the first time a ceiling has moved on this surface, which is what that ceiling exists to make visible. `get_context`'s per-call hint stays: it is ~340 bytes per response rather than resident, and 0069 put it there for hosts that drop instructions. No grace window — [0088](0088-a-retired-name-answers-for-one-release.md) forwards a renamed *name*, and what changed here is arguments, so there is nothing to forward. |
| [0077 One address for a file](0077-one-address-for-a-file.md) | Accepted; the current record for how the CLI names a file | `ochakai attach` and `ochakai detach` are folded into `ochakai put` and `ochakai delete` (CLI 26 → 24, no new flag). They were a second address: they named a file by `<concept id> <name>` rather than by the bundle path it lives at, in a word the rest of the product had retired. A write's kind is decided by its bytes, exactly as the server decides it — frontmatter with a `type` is a concept at `<id>.md`, anything else is a file at the path given — so this is the CLI mirroring REST rather than one command doing two things. A delete has no bytes, so the argument's spelling picks which of the two addresses to ask first and the other is tried only when the first holds nothing. The relative markdown link `attach` printed is kept by `put`: attribution is derived from bodies, so a file nothing links is a file nobody finds. What folds away is convenience — several files per invocation (a shell loop) and `--name` (the path is the name). The web UI's buttons follow the CLI's word, which is what 0064 §8 asked for when it kept them symmetric. |
| [0004 The remote CLI](0004-cli.md) | **Superseded by 0067** | |
| [0007 Retiring the direct-database commands](0007-api-only-cli.md) | **Superseded by 0067** | |
| [0015 Surface consistency](0015-surface-consistency.md) | **Superseded by 0067** | |
| [0058 Two filters nobody arrived through](0058-filters-nobody-arrived-through.md) | **Superseded by 0068** | |
| [0033 Context hits are a ranking](0033-context-hits-are-a-ranking.md) | **Superseded by 0067** | |
| [0102 One history, in one spelling](0102-one-history-in-one-spelling.md) | Accepted; **breaking**; amends [0064](0064-rest-stops-at-api-v1.md) §11 and §14.1 | **`?history` answers JSON whatever the Accept says**, and the markdown rendering of it is gone. OKF SPEC §9 defines one markdown history — the reserved `log.md` beside the objects — and this address rendered a second one from the same ledger rows. The duplication mattered less than what it did to `limit`: as 0064 §14.1 recorded, one `?history&limit=500` on a concept was a 400 as JSON and a 200 as markdown, and that section's decision was to *describe* the split rather than close it. What is lost is reading one object's history as markdown, and no surface in the product used it — `ochakai revisions` and the web UI's history panel both read JSON. The other half is the freeze: 0064 §11 gains a **third** reason it may be broken — folding away a second spelling of something the format already defines — admitted only when all three conditions hold (the spec defines the spelling, ochakai carries a second one, and the one folded is ochakai's). The record says plainly that this is **weaker than the other two**, because "is it the same thing" is a judgment on ochakai's side rather than a yes/no an outside document answers; hence the three conditions, and hence a representation may be folded while an address may not. The fingerprint does not move — the `text/markdown` line is shared with the concept export — so the record and the changelog are the only places this is written down. |
| [0101 A level can be walked](0101-a-level-can-be-walked.md) | Accepted; amends [0082](0082-what-the-freeze-holds-still.md) §2 and [0075](0075-the-bundle-is-the-address-space.md) §4.2 | **The JSON listing of a level takes a `cursor`.** [0068](0068-how-a-face-is-added-and-removed.md) §2.1 had already decided what a listing owes — "the guarantee is not walking it to the end, it is not stopping silently at a cap" — but the bundle listing is a different operation and the rule never reached it: it cut at 1000, set `truncated: true`, and offered no way past. Not silent, but a wall. **The page size does not move**: at 1000 it stays, so a caller that fits under it today receives exactly what it receives today and only the ones that hit the wall gain anything. One cursor carries **two runs** (concepts by id, files by path — they end independently) and **the directory**, since resuming one level from another's position would skip rows in both; the subdirectories are a `GROUP BY` with no position to resume from, so they ride on the first page only. The markdown `index.md` does not page — SPEC §8 has no spelling for "continued" — so a `?cursor=` there is a 400 rather than a silently repeated first page. The other half is **the freeze**, which needing one new query parameter exposed: [0082](0082-what-the-freeze-holds-still.md) placed response-side additions outside it, but 0064 §2 closed unknown keys with a 400 expressly to "decide whether anything can be safely added after the freeze", so **an optional query parameter was never what the freeze was holding still** either. Three narrowings keep that honest: `query` only (a header can be added by a relay, a path parameter is an address), `required=false` only (a required one is a 400 for every client that predates it), and whole parameter lines only. `truncated` stays — removing a field from a response is the freeze itself, so that half is 1.0's work, the same trade [0097](0097-a-write-says-what-it-did-in-the-body.md) §2 made for `Ochakai-Plan`. The fingerprint grows by two lines; no counted surface moves. |
| [0100 `.md` is how a concept is spelled](0100-md-is-how-a-concept-is-spelled.md) | Accepted; **breaking**; amends [0075](0075-the-bundle-is-the-address-space.md) §3.3 and [0064](0064-rest-stops-at-api-v1.md) §11 | A `.md` address may hold only a concept or a reserved name (`index.md`, `log.md`), and a PUT of markdown carrying no `type` becomes a 400 instead of being stored as a file at that address. The motive is conformance: today's write path keeps a typeless `.md` **at that same address** as a file and the export carries it, so the bundle that comes out fails OKF SPEC §11.1 and §11.2 — every non-reserved `.md` must have parseable frontmatter with a non-empty `type`. The five rejections §11 forbids a consumer (missing optional fields, unknown `type`, unknown extra keys, broken links, a missing `index.md`) do not include a file with no frontmatter at all, so [0075](0075-the-bundle-is-the-address-space.md) §3.3 extended that tolerance past its own edge — and here ochakai is the producer, which is what §11's conformance binds. Stored files named `.md` are renamed to `.markdown` by a migration: the bytes still come out, only the spelling of the address moves, so 0075 §2's invariant survives; the links that break are the one break SPEC §6.1 requires consumers to tolerate. Import renames and reports rather than refusing, because losing data on a restore is worse than an odd address. It **breaks the freeze**, so it also asks for a second reason to be allowed to: **output that does not conform to OKF**, narrowly scoped to SPEC §11's three MUST conditions, so the judgment is a yes/no an outside document answers rather than something ochakai can argue itself into. The sharp part: this breaking change moves **zero lines of the golden fingerprint**, because OpenAPI cannot spell a `requestBody` that differs per address — 0064 §21's "the freeze can hold what exists, not which one you get" arriving in full. No counted surface changes, and three things fold away with it: 0064 §20.4's narrow gap (a precondition unread on the DELETE that reached a file through the concept branch), §16's rule that the body decides an object's kind, and `missingObject`, which existed to keep a concept's 404 from being answered as "this deployment holds no files". |
| [0082 What the freeze holds still](0082-what-the-freeze-holds-still.md) | Accepted; amends [0064](0064-rest-stops-at-api-v1.md) §11 | The freeze covers **addresses, the shape of a request, and removals or changes in a response** — adding a property to a response-only schema was never what it was holding still. 0064 §2 closed unknown query keys with 400 expressly to decide "whether anything can be safely added after the freeze"; on that footing a client ignores a response key it does not know, and `required` in a response is a promise the server makes rather than a demand on the caller. The restriction to response-*only* schemas is the one subtlety: a schema reachable from a `requestBody` cannot grow a required property without producing the 400 that 0064 §2 introduced, and which schemas those are is derivable from the contract. Additions still regenerate `api/openapi.frozen.txt`, so they still appear in a diff a reviewer reads — what changes is which regenerations are legitimate and what the failure says. Prompted by C7: the loop's own instruments (dropped observations, outcome coverage, throughput) have nowhere to go but `stats`, and the CLI and web UI are REST clients (0067 §2), so the freeze was holding the product's central claim still rather than its contract. Requests, removals, renames and type changes are untouched. |
| [0083 An error carries a code](0083-an-error-carries-a-code.md) | Accepted | Every error response now carries `code` beside `error`: the sentence is for a person and stays free to be reworded in any release, while the code is a closed vocabulary of twelve words a client may branch on. Three conditions answer 409 — `already_exists`, `not_deleted`, `no_rejection` — and telling them apart meant matching prose that `docs/compatibility.md` explicitly allows to move, so an integrator embedding the REST API (C6) had only a method known to break. The code repeats the condition even where the status already determines it: a key present on only some errors forces a client to ask whether this failure has one, which is not something you can switch on. The mapping lives in one place and is read from the sentinels the service already returns, so the sixty-odd call sites are unchanged. RFC 9457 (`application/problem+json`) is declined for three reasons: it replaces the media type, which is the part of the contract 0064 froze hardest; its `type` URI would put a domain's survival inside the contract, which a product whose first condition is that the knowledge stays the user's cannot do; and it carries the same information under more keys. This is an addition to a response-only schema, which [0082](0082-what-the-freeze-holds-still.md) places outside the freeze — the golden fingerprint is regenerated in the same PR. VOCAB rises 34 → 46. |
| [0084 A hit says why it matched](0084-a-hit-says-why-it-matched.md) | Accepted | A search hit now carries `snippet`: the passage of the body where the query's own words land, cut to a readable window and marked where it was cut. Title and description say what somebody wrote about the concept as a whole, not what answered this question, so a caller choosing among ten hits had to fetch bodies — for an agent, spending a fixed context budget reading nine documents it did not want. That is the same arithmetic 0067 §4 used when it took documents out of result lists, one step further along. It is absent whenever there is no passage to point at: the words landed in the name or the description, where the row already shows the match, or the concept was found by meaning rather than text, where the opening of the body would answer "why did this match?" with something that is not the reason. Listings have no query and so no snippet. Cut in Go rather than with `ts_headline` because the top N bodies are already in hand (scoring passes ids and booleans through the window functions and fetches entries afterwards) and because the unit of a match is migration 0036's two-character window, which is this side's rule. REST, CLI and the web UI show it; MCP responses carry it but its tool descriptions do not mention it, since a key that appears in the response is its own documentation and schema prose is paid for out of the agent's context. |
| [0085 The empty base, and what fills it](0085-the-empty-base-and-what-fills-it.md) | Accepted | `ochakai seed` reads the answer to a query the operator ran themselves — `INFORMATION_SCHEMA.COLUMNS` rows, as a JSON array or one object per line — and prints an OKF bundle of `BigQuery Table` **drafts**; `ochakai import` writes them, so no second write path appears. It connects to no warehouse: no warehouse client is linked into the binary and no credential is read, so [0081](0081-what-ochakai-is-and-what-it-refuses-to-hold.md) §1's refusal of connector ingestion is untouched, and the command serves any warehouse whose catalog can be spelled as those rows. Every concept comes out as a draft with an empty description, and that is the point: what a table is for, which column lies and when the load is late are the reasons anybody reads the entry, the schema knows none of them, and a sentence nobody wrote is one everybody would trust. A catalog's connector produces thousands of rows nobody vouches for; this produces thousands of questions. Declined: linking a BigQuery client into the binary (a connector product under another name), a server-side projection endpoint (credentials reach the server), and folding it into `import` as a flag (one command, two capabilities). CLI 24 → 25, FLAG 28 → 29. |
| [0086 A second way to say who is calling](0086-a-second-way-to-say-who-is-calling.md) | Accepted; amends [0003](0003-gcp-only.md); §4 amended by [0117](0117-a-person-recorded-as-a-process-says-so.md); header amended by [0121](0121-each-path-reads-its-own-header.md) | A deployment that sets `OCHAKAI_OIDC_ISSUER` and `OCHAKAI_OIDC_AUDIENCE` verifies bearer tokens itself — signature, issuer, audience, expiry — against the issuer's published keys. Off Google Cloud there had been two postures and neither is a deployment: `dev` authenticates nobody and `read-only` authenticates nobody and refuses every write, while everything in between rested on Cloud Run's IAM check having already happened in front of the process. **No secret is introduced**: verification reads public keys over HTTPS, and the algorithm allowlist is asymmetric only, so there is still nothing to issue and nothing to rotate — which is what 0003 was protecting, rather than Google Cloud itself. Unchanged: there is still no authorization (whoever the issuer vouches for can read and write everything), embeddings are still Vertex AI or nothing (off Google Cloud search is lexical, which since migration 0036 looks Japanese terms up in an index rather than scanning for them), and delegation follows the same rules on both paths. A verified email is recorded as a person; an email the issuer will not vouch for is never used, and the subject is recorded as a process instead. Half a pair, or a pair beside `dev`/`public`, is refused at startup. ENV 11 → 13. The contract also gains one retroactive declaration: the 401 both paths already sent is now declared on every operation (`components/responses/Unauthorized`, `code: invalid`), and the spec's prose states both postures instead of claiming ochakai itself verifies nothing. |
| [0121 Each path reads its own header](0121-each-path-reads-its-own-header.md) | Accepted; adds to [0086](0086-a-second-way-to-say-who-is-calling.md); scopes [0065](0065-identity-and-provenance.md) §2 | **A deployment that verifies its own tokens reads `Authorization` and nothing else.** 0065 §2 said to prefer `X-Serverless-Authorization` over `Authorization`, and that rule kept applying to both paths after 0086 added the second one — so **an OIDC deployment behind Cloud Run IAM answered 401 to every request**, picking up the Google token Cloud Run forwards with its signature stripped and trying to verify it against the operator's issuer, while the caller's real credential sat unread in `Authorization` of the same message. The failure reads as cryptography ("signature does not verify") for what is a header in the wrong place. **And that configuration is Google's own two-header split working as documented**: a client that must satisfy Cloud Run IAM *and* carry an application token puts the Google token in `X-Serverless-Authorization` and its own in `Authorization` — ochakai's own `serve-ui` is written that way. The precedence keeps its reason on the Cloud Run path, where only one of the two headers was validated in front of the process and reading the other would let an authorized caller be recorded as somebody else; **on the OIDC path that reason is absent**, because neither header was validated by anything and ochakai verifies whichever token it is handed, so preferring Google's only shadowed the caller's credential with one this issuer never minted. A request carrying only `X-Serverless-Authorization` is refused by name rather than by signature failure, the shape [0117](0117-a-person-recorded-as-a-process-says-so.md) set. **Nothing else moves**: the verification, the recorded name, delegation, secret-zero, and no counted surface. The configuration this repairs — an operator's own IdP for identity, Cloud Run IAM for reachability — stands on its own; **the publicly invokable posture is deliberately not spelled here**, being [0119](0119-an-operated-fleet-is-deployments-or-directories.md) §5's first item and a decision that moves the deploy guide, SECURITY.md and the rate limiting [0066](0066-four-postures-one-word.md) §7 declined. Declined: reading the Google header as a fallback (a path that must always 401 is not a path), trying both and taking whichever passes (which would make who a write is recorded as vary per request), changing the Cloud Run path, and refusing this at startup — a process cannot see whether it sits behind IAM, and after this record that configuration is the correct one. |
| [0087 A sandbox says it is one](0087-a-sandbox-says-it-is-one.md) | Accepted; extends [0066](0066-four-postures-one-word.md) | `OCHAKAI_MODE=sandbox` deploys the fourth cell of the posture square on purpose: anonymous, writable, and disposable. The loop is what this product is about — an agent drafts, a human rules, an outcome comes back — and it was the one thing nobody could try: the public demo is `public`, which implies read-only, so seeing a write-back at all meant standing up Postgres. It is not `dev` under another name: `dev` says in its own documentation that it is not for a deployment, an operator reading a config cannot tell an accident from an intent, and the Terraform module grants `allUsers` only to a public posture. §3 is the point — **a sandbox that does not announce itself steals the work somebody curated into it** — so `GET /api/v1/stats` answers `sandbox: true` and the bundled web UI carries one banner on every page. A header would have been the read-only precedent, but the wire is frozen and [0082](0082-what-the-freeze-holds-still.md) places response *schema* additions outside the freeze, not new header names; `stats` is already the face that answers about the loop. Misses are not recorded, for the reason a public deployment does not record them. |
| [0112 A start refuses a variable it does not read](0112-a-start-refuses-a-variable-it-does-not-read.md) | Accepted; **BREAKING**; applies [0064](0064-rest-stops-at-api-v1.md) §2 to the other surface an operator spells by hand | **A variable in ochakai's namespace that ochakai does not read stops the start, by name.** The product has refused a bad *value* many times over — an unreadable `OCHAKAI_MODE` is a startup error rather than a guess (0066 §4), `OCHAKAI_EMBEDDINGS=false` would have paid Vertex AI for every write it was asked not to make (0080 §2), a malformed principal in `OCHAKAI_ADMINS` is refused (0109 §3) — and had **no check at all on the name**: `OCHAKAI_RECORD_MISSES` typed one letter short left the recording on with nowhere to see that it had, and a deployment that kept `OCHAKAI_VERTEX_PROJECT` after 0078 folded it away kept a value nothing consulted. That is the hole 0064 §2 closed on the wire, where an unrecognized query key became a 400 naming it, expressly so something could be added later without breaking a client. Here: `serve` and `serve-ui` — the two processes configured by environment — list every unknown name at once and refuse to start, and the list of what ochakai does read (`config.Known`) is read back out of the `os.Getenv` calls rather than trusted (0035), because a variable missing from it is one the next deployment cannot set. **`OCHAKAI_TEST_` is the harness's half of the namespace** and is skipped — `scripts/check --db` exports `OCHAKAI_TEST_DATABASE_URL` across the whole test run — with a second test holding that nothing shipped reads under that prefix, since a named exception is a place to hide something. **The other residents the server never reads are the ones ochakai ships a non-Go reader for** — `OCHAKAI_RECALL_LIMIT` in the recall hook, `OCHAKAI_ID_TOKEN` in the BigQuery job — kept as a list (`config.KnownElsewhere`) rather than a prefix, because readers exist under those names and a prefix would let a typo through; the list is read back out of `examples/` and `.claude/` rather than trusted, which is how a retired `OCHAKAI_INSECURE_DEV` still being taught by an example was found. **Environment variables get no deprecation window**: 0088's one release is for names a caller is told about at the call, and for a variable "the old spelling still works" *is* the silent breakage, so being named at startup is what takes the window's place. The CLI's client commands are not checked — a wrong `OCHAKAI_URL` announces itself immediately, where a server serves a posture nobody asked for for months. Rejected: a warning log (nobody reads the startup log of a service that is running), and a table naming only the retired spellings (it grows forever and catches no typo at all). Breaks a deployment carrying a stray `OCHAKAI_*`; on Cloud Run a failed revision takes no traffic, so the cost is that the new configuration does not ship, not that the service falls over. No surface count moves — ENV stays 15. |
| [0088 A retired name answers for one release](0088-a-retired-name-answers-for-one-release.md) | Accepted | The stability contract for MCP and the CLI, where there had not been one: the policy said "no deprecation window for any of them", which was honest and still sent the notice to the wrong reader — an agent's configuration names tools literally, and it is people, not agents, who read a changelog (C5, C4). A release that renames an MCP tool or a CLI command now keeps the old spelling answering **for that release and no longer**. §3 is the point — **callable, but not listed**. `tools/list`, the usage text, `docs/cli.md` and the completion scripts all keep quiet about the old name, so an agent that never learned it never will, and nobody pays twice for a tool description out of a context window ([0067](0067-four-faces-and-what-they-decline.md) §1); that is why the forwarding is middleware rather than a second registration, since registering it is what puts it in `tools/list`. Each entry carries the release that renamed it, and a test fails once that release is no longer the newest in the changelog — so closing the window is the release PR's job rather than anyone's memory (§4: a deprecation window fails by never closing, not by being broken). Nothing is retroactive (§5): the names MCP dropped earlier stay dropped. Names only — not flags, not output shapes, not the stored form, which is one-way. This is the counterweight to what [0082](0082-what-the-freeze-holds-still.md) loosened on the REST side; no surface count moves, because the counts read `tools/list` and the command table, which is what a user actually pays. |
| [0125 A body names each field once](0125-a-body-names-each-field-once.md) | Accepted; makes [0064](0064-rest-stops-at-api-v1.md) §2 hold as written; applies §20.2 to the body | **A JSON body key is matched exactly, and a key given twice is a 400 naming it.** Not a new rule — 0064 §2 decided that an undeclared body key is a 400 naming it, and the mechanism meant to enforce it only ever did half the job: `encoding/json` matches names without regard to case, so `{"RULING":"verified"}` reached a handler that had declared `ruling` and was answered 200. `DisallowUnknownFields` reports keys that match no field and **says nothing about a key that matches one it should not**, which is the same false-green shape inverted — instead of a key the server does not know being dropped, a spelling the contract never declared is accepted. The same function silently took the last of a repeated name, which is the question 0064 §20.2 already answered for query keys (`?limit=10&limit=50` has no answer that is what was asked); here the direction is fixed rather than self-contradictory, which is worse in the one way that matters — the caller gets a decided answer without knowing which of the two values decided it, on the operation that records a ruling. The query side's exception for keys declared as arrays has no counterpart: JSON spells a set as an array, so repetition never needed a second meaning. The mechanism is `encoding/json/v2` (Go 1.27), whose defaults refuse both and which carries the offending name as structured data, retiring the string surgery that used to recover a field name from an error message. **Only the reading side moves** — v2 changes marshaling defaults too (`omitempty`, nil slices, time formats) and those are bytes the frozen surface actually ships, so responses stay on v1 and the package holds both under distinct names. 0107's frozen core is untouched: the four operations that read a body (`move`, `review`, `usage`, `access`) sit outside it. The contract gains two paragraphs so that query and body state the same rule in the same words. Declined: migrating the whole tree to v2, the MCP tool arguments (decoded by the SDK, not by ochakai), and a 0088 window (nothing was renamed). |
| [0107 The freeze holds the OKF core](0107-the-freeze-holds-the-okf-core.md) | Accepted; narrows the scope of [0064](0064-rest-stops-at-api-v1.md)'s freeze | **The REST freeze narrows to the OKF core** — the bundle round trip (`GET`/`PUT`/`DELETE /api/v1/bundle/{path}`) and `GET /api/v1/search`; the rest of `/api/v1` returns to the 0.x-unstable regime MCP and the CLI were already under (a breaking change is marked BREAKING, carries a design record, ships in a minor). This is **a retraction of a published promise, the first of its kind here**, and the record calls it that rather than reusing 0082's "the freeze never held this" framing, which is unavailable. Grounds: the freeze needed four corrections in fourteen days and every one pointed at OKF (the boundary was drawn around whatever the implementation carried on 2026-08-02, not around a designed core), and 0082 §5's own arithmetic — external users effectively zero, the price of fixing a defective rule never gets cheaper. Membership is a criterion, not a list: what is frozen is the set of operations that touch the knowledge being there at all — reading, writing and deleting the bundle address space, and the one read entry that finds things in it. The narrowed promise is harder than the old one: the core can only ever widen, and a record shrinking it again cannot be written — that would be a different product, not a revision. Not a byte of the wire moves; what moves is `docs/compatibility.md`, the changelog preamble, and the fingerprint's scope (`api/openapi.frozen.txt` now reads the core alone). |
| [0064 REST stops at /api/v1](0064-rest-stops-at-api-v1.md) | Accepted; amends [0046](0046-bundle-address-space.md) §3.5, [0057](0057-concept-is-the-word-a-reader-meets.md) §3.2, [0071](0071-the-recommended-type-vocabulary.md) §1 and [0074](0074-the-document-and-the-vocabulary-that-asks-it.md) §§3, 7 | The last breaking REST batch before the wire is declared frozen (issue #379); `docs/compatibility.md` is rewritten in the same PR to say so. **Breaking**: unknown query parameters now 400 on every operation (`fm.` excepted); `Ochakai-On-Behalf-Of`/`Ochakai-Producer` lose their `X-` prefix; a file's own address answers `Accept: application/json` with metadata instead of ignoring the header; `attachments`/`Attachment` are renamed to `files`/`File` on the wire and the MCP tool `get_attachment` becomes `get_file`; the `entries` JSON key on `stats`, the bundle listing and `context`/`get_context` is renamed to `concepts`, revoking 0057 §3.2's exclusion of it (issue #411 — the exclusion's reasoning did not hold for two of the three places it applied); the `change` vocabulary's last retired-word holdouts, `attach`/`detach`, are renamed to `add_file`/`remove_file` with a migration rewriting stored rows (§8, issue #470); `/context`'s redundant `truncated` count is dropped (it always equaled `len(outline)`), the bundle listing's file rows say `created_at` instead of `updated_at` (files are content-addressed and immutable), and `reembed`'s `embedded` is renamed to `concepts` to match its sibling `files` (§9, issue #470); a failed `If-None-Match: *` is 412 not 409; DELETE on a concept honors `If-Match`; a file PUT answers 201 on creation; `ruling: withdrawn` with nothing to withdraw is 409 not 404; out-of-range `limit`/`days` is 400 everywhere instead of silently clamping. `.md` is mandatory and part of a concept's bundle path, not a representation selector; `Change.path` is the full bundle path, not bare. Amends 0046 §3.5's representation table: a concept's default (no `Accept`) is the JSON View, not the export form. Also breaking, and the last chance for each: `title` is optional everywhere on the wire (OKF SPEC §4.1 gives it no default, so a resolved title asserted a value the document never declared; `status` stays resolved because SPEC §5.4 does give *it* one), `observed.generated.at` reports the content's last meaningful change rather than the row's write time (SPEC §5.2 — the exported document always rendered the right one and only the JSON differed), and a `type` may contain `/` (SPEC §4.1/§11 require tolerating unknown types; the ban was ochakai's own, amending 0071 §1 and 0074 §§3, 7 on that one point). Additive in the same batch: the PUT finally declares a `requestBody` — no generator could build a write client without one, and the contract test could not see the gap because it skipped every bundle PUT — plus the `Ochakai-Read-Only`, 400, 413, `Cache-Control`, `X-Content-Type-Options`, `Content-Security-Policy` and 304 `ETag` declarations the server already sent (HEADER 10 → 13). Only a security defect justifies breaking the freeze after this point; a repeated DELETE on an already-gone id stays 404, deliberately, and is safe to retry. No version or capability signal ships, and CORS stays deliberately absent.
| [0103 The tool result travels once](0103-the-tool-result-travels-once.md) | Accepted | **MCP tools declare no `outputSchema`, and a tool's answer comes back as one text content block.** Measurement is the motive: MCP-BYTES policed the resident cost of the tool list to 26 bytes of its 13,500 ceiling while counting only name, description and *input* schema — the seven output schemas it never counted came to 14,986 bytes, more than everything it did. The same doubling was on every call: one `get_context` carried the identical JSON in `content[0].text` (10,384 bytes) and in `structuredContent` (10,439), for a 22,009-byte response. None of it was written by hand — the go-sdk derives an output schema from the handler's return type, fills `structuredContent` when there is one, and adds the text copy as the backwards-compatibility fallback the MCP spec recommends, so the duplication was conformant and the schema was a side effect of a type annotation. Passing `any` to the SDK and appending the JSON as text removes both. What is lost is typed output for clients that read `structuredContent`; the bytes are identical and text is the one content type every MCP client can read. Declined: keeping the schema and filling `Content` by hand (pre-SEP-2106 clients would get an empty block — closing the spec's own escape hatch), and raising the ceiling to 28,500 to count both (counting the right thing, but [docs/surface.md](../surface.md)'s third question asks what can be folded away, and something could). The budget now counts both schemas; the number does not move, because the output side is empty. |

## The verification loop and usage measurement

| Record | Status | Decision |
|---|---|---|
| [0069 The loop, and what measures it](0069-the-loop-and-what-measures-it.md) | Accepted; the current record for the verification loop and usage measurement (carries 0025, 0029, 0037, 0049, 0051, 0059) | Three queues a curator can empty — unreviewed drafts, unanswered failure reports, concepts past their declared expiry — beside `sort=verified_at`, which is a ranking nobody can zero and is deliberately not counted. Evidence-based staleness sits next to time-based: a `failed` report outranks age, and the feed drops an entry once it is verified again. `stale_after` is the writer's own declaration, so verifying does not clear it — only re-declaring the date does. Usage events are buffered off the read path and are explicitly best-effort; a search that returns nothing becomes a row of its own (zero hits, never a low score), capped at 500 bytes and pruned at 180 days, off on a public deployment. `GET /api/v1/stats` answers the instance in one call, separating state from flow, with `prefix` scoping everything except misses — a miss has no id to hang off. No rollups, no scheduler, no delivery: the exit code and the counts are what an operator's cron reads. Consolidates six records; no decision changes. |
| [0090 A queue ranks on what happened lately](0090-a-queue-ranks-on-what-happened-lately.md) | Accepted; amends [0069](0069-the-loop-and-what-measures-it.md) | The usage counters are running totals and never decay, so ranking a queue on them left a concept that was read a lot in a knowledge base's first month at the top of the promotion queue for as long as the deployment lived — a queue whose job is "look at this next" answering "look at what you already looked at" — and a long-ago repeat offender above the concept failing this week. `sort=usage` and `sort=failed` now rank on the last 90 days; the lifetime totals stay and break the ties. The counts reach the wire as `usage.recent` and **carry their own window**, because a count whose period is implicit is one a reader cannot check (the rule [0089](0089-a-half-embedded-concept-says-so.md) set for `stats.embedding`). The window is a constant rather than a setting — it is the product's answer to "how long does a read go on counting as demand", and making it per-deployment would mean the queues mean something different in each one — and it has to sit inside the 180-day retention of the raw events it is counted over, or it would read as a period of time and mean "as much as we still have". No stored decay column: that needs something running on a schedule, and this product has no such moving part; `knowledge_event` already holds the rows with their timestamps, indexed for exactly this range scan. `search_hits` is not windowed because it is not demand — one is recorded for every id in every result set, so windowing it would only make the ranker's recent output the recent demand. The cursor now walks an order that drifts as counts age out, which the record states rather than hides. |
| [0095 The loop has a shape](0095-the-loop-has-a-shape.md) | Accepted; amends [0069](0069-the-loop-and-what-measures-it.md) | The human half of the loop gets a trend: eight seven-day buckets of verifications, oldest first, on `stats.review.weekly`. What was asked for was queue depth *and* verification rate over time — **queue depth is not recoverable**. All three queues are counted from current state and nothing snapshots how deep one was last month; rebuilding it from revisions would produce a number that looks right and is not, which 0089 §4 declined for the same reason. And 0069 had already written the distinction: *"a queue says how much is left, never how much went through."* So the thing that can be measured turned out to be the thing worth measuring — verifications sit in an append-only ledger with their times and are never pruned, so the shape is recoverable for as long as the base exists. Seven-day buckets counted back from today rather than calendar weeks, because a calendar week makes the newest bucket a partial one whose dip is the calendar rather than the curator. The length is a constant and does not follow the caller's `days`: a shape whose length changed per request is two readings that cannot be compared. **Absent on a base nobody has ruled on yet** — eight zeroes read as "review stopped", and "review has not started" is a different thing to say — while a week with no rulings inside an active base is a zero in the row, because grouping the ledger alone would drop that week and draw its neighbours as adjacent. The web UI draws one figure on the review page with no axes, no gridlines and no legend, answering one question (is reviewing picking up, holding, or stopping); anything more would be the BI tool 0067 §1 said this surface is not. Not on MCP. |
| [0104 A ruling travels with its reason](0104-a-ruling-travels-with-its-reason.md) | Accepted | **A rejection's reason now reaches `ochakai get` and the exported bundle.** `reject --note` calls itself the thing "the next agent reads before proposing again", and the README calls a rejection "a memory of *no*" — but the note reached REST, MCP, the web UI and `ochakai context`, while `ochakai get` printed a rejected concept as an ordinary draft and the bundle carried `rejected_by` and `rejected_at` without the why. Of a ruling's three parts, the reason is the only one the next writer can act on: who and when are audit, and nothing else says what would have to change for the next proposal to land. So export emits `rejected_note` beside the other two — the same ochakai extension key, still never read back on import, so 0009 §3.2's "a bundle carries knowledge, not one instance's judgment" is untouched — `ochakai get` prints the ruling on stderr next to the provenance line, and a result set containing rejected concepts says so in one stderr line. **Neither stdout nor the row format changes**, and no command, flag, endpoint or tool is added. Declined: putting the note on search hits (it fattens a frozen response and puts prose in a ranking row), printing it on `get`'s stdout (that breaks the document round trip), and making `--note` mandatory (this record is about a written reason not being lost, not about compelling one). |
| [0050 Listings page, rankings do not](0050-listings-page-rankings-do-not.md) | **Superseded by 0068** | |
| [0062 A listing is not a search](0062-a-listing-is-not-a-search.md) | **Superseded by 0068** | |
| [0056 One question, one command; one ruling, one word](0056-one-question-one-command.md) | **Superseded by 0068** | |
| [0055 One ruling, one face](0055-one-ruling-one-face.md) | **Superseded by 0056, then 0068** | |
| [0049 Counting the review queues](0049-queue-counts.md) | **Superseded by 0069** | |
| [0059 A queue is named by the listing that shows it](0059-a-queue-is-named-by-its-listing.md) | **Superseded by 0069** | |
| [0025 Closing the write-back loop](0025-closing-the-loop.md) | **Superseded by 0069** | |
| [0029 Usage recording off the read path](0029-usage-recording-off-the-read-path.md) | **Superseded by 0069** | |
| [0051 Recording the questions nothing answered](0051-instance-metrics-and-search-misses.md) | **Superseded by 0069** | |

## Concurrency and deletion

| Record | Status | Decision |
|---|---|---|
| [0030 Optimistic locking with If-Match](0030-optimistic-locking.md) | Accepted | Conditional updates are opt-in, exposed as an ETag and accepted as `If-Match`; a mismatch is a 412 with nothing written. Send `If-Match` and handle 412; omitting it keeps last-write-wins behavior. |
| [0031 Purge](0031-purge.md) | Accepted | A second deletion stage that discards an already soft-deleted entry and frees its id, refusing a live one. Irreversible, REST/CLI only — an export is the only way back. §3.2's refusal to reclaim GCS bytes is amended by [0099](0099-a-purge-reaches-the-bytes.md). |

## Semantic models and compile

| Record | Status | Decision |
|---|---|---|
| [0070 What was retired, and the bar for retiring it](0070-what-was-retired-and-why.md) | Accepted; the current record for what ochakai decided not to do (carries 0012, 0018, 0028) | Three faces came down — the public MCP OAuth connector, deterministic SQL compilation, and any dedicated mechanism for semantic models. In none of them was the design wrong; what flipped was the cost side, and it flipped the same way each time: no users materialized, the documentation had already demoted the feature, and the maintenance surface kept accruing (the connector alone carried the deployment's only secret and its only public service). **Retiring is maintenance, not failure.** No user knowledge is deleted: retired types stay first-class as free types and past usage rows stay. Consolidates three records; no decision changes. |
| [0018 Retiring import-ossie](0018-semantic-model-as-knowledge.md) | **Superseded by 0070** | |
| [0028 Retiring compile_sql](0028-retire-compile-sql.md) | **Superseded by 0070** | |

## The MCP OAuth connector, and what replaced it

| Record | Status | Decision |
|---|---|---|
| [0116 The connector's price changed, its condition did not](0116-the-connector-price-changed-not-its-condition.md) | Accepted; amends [0070](0070-what-was-retired-and-why.md) §5 | **Reimplementing the retired MCP OAuth connector no longer starts from [0010](0010-mcp-oauth-connector.md)'s authorization server.** 0012 retired that service on 2026-07-18 for costing a deployment its only secret, its only public service, its only Domain Restricted Sharing exemption and the attack surface of a public AS; [0086](0086-a-second-way-to-say-who-is-calling.md) landed three weeks later and meant ochakai never needed to *be* an authorization server. Measured: a deployment publishes RFC 9728 protected-resource metadata and a `WWW-Authenticate` challenge on `/mcp`'s 401 — and the two facts that document must carry are the two `OCHAKAI_OIDC_AUDIENCE` and `OCHAKAI_OIDC_ISSUER` already name. A vendor-hosted client found a cross-host authorization server from the document alone, registered itself there unaided, returned with a token bound to this audience, and its write was recorded as the person the issuer vouched for. **863 lines of connector become about 180, with no new environment variable and no new posture.** **It is still not adopted.** Of 0070 §1's four observations, three do not arise; the fourth — *no use* — has not moved, and it is the whole of why 0012 retired the thing. What got cheaper is the implementation, not the demand, and adoption would take on the homework around it: a publicly invokable deployment the deploy guide currently forbids, rate limiting and abuse handling 0066 §7 declined, SECURITY.md's threat model, and the operator's own IdP configuration — where the price actually moved (unaided registration is off by default at an issuer; a client sends `scope: openid` at registration whether or not scopes are advertised; without a verified email in the *access* token a person is recorded as a process, which [0117](0117-a-person-recorded-as-a-process-says-so.md) takes on its own). 0070 §5's condition for revisiting is unchanged — only what you open on that day. |
| [0010 An MCP OAuth connector service](0010-mcp-oauth-connector.md) | **Superseded by 0012, then 0070** | |
| [0012 Retiring the connector](0012-retire-mcp-oauth-connector.md) | **Superseded by 0070** | |
| [0039 A stdio bridge for MCP clients](0039-mcp-stdio-bridge.md) | **Superseded by 0067** | |

---

Two numbers are vacant. **0026** was a proposal to replace Cloud SQL with
Cloud Storage as the only persistence layer; it never landed, so it lives
in its pull request. **0034** is described above.
