# ochakai 改善計画(2026-08)

これは維持者のための計画文書である。マニュアル(docs/surface.md の DOC)には
数えない — CHANGELOG や CONTRIBUTING と同じ「変える人の面」に属する。

依頼は「リポジトリとプロダクトのイケてないところを洗い出し、類似プロダクトを
調査・比較し、改善計画を立てる。リポジトリの自己制約は無視してよく、破壊的
変更も許容する」。調査は四本並行で行った: コードベース監査(全パッケージ、
file:line 単位)、semantic layer / verified-query store の 2025–2026 動向、
エージェントメモリ層とコンテキスト基盤の動向、カタログ製品と日本市場と
OSS 配布戦略。外部調査の出典 URL は各エージェントの報告に残っており、
本文では結論だけを引く。

## 0. 要約

**プロダクトの設計思想は 2026 年の外部環境にむしろ裏付けられている。**
LLM を内蔵しない・SQL を実行しない・人間の裁定を一級にする、という賭けは、
競合全員が逆を張った(そして誰も人間検証層を作らなかった)ことで、空いた
枠として残っている。OKF v0.2 が trust 段階を仕様に取り込んだことも追い風で
ある。問題は思想ではなく、次の四つの実行面にある。

1. **配布がゼロに近い。** GitHub 2 stars・0 forks。Zenn / Qiita / X /
   はてなで言及ゼロ。MCP レジストリ・Smithery・awesome 系リストのどこにも
   載っていない。維持者の登壇資料(数万 view 級)から repo への導線が無く、
   いちばん安い漏斗が 100% 漏れている。製品のどの弱点よりもこれが大きい。
2. **核(検索と改善ループ)が計測されていない。** 検索品質の評価基盤が
   存在せず(ランキングのテストは 2 本)、C7 の「測れるループ」は
   ランカー自身の出力を需要として数える自己参照になっている。
3. **エージェントと人の両方に、日々の摩擦が残っている。** MCP は常駐
   3〜4k トークンを課し、`get_context` は N+1 で budget 契約を守らず、
   エラーは英語散文のみ、Web UI は 2,400 行の単一ファイルでテストも
   a11y も日本語も無い。
4. **プロセスが一人の速度を食っている。** 散文の総量がコードと同規模に
   達し、天井の管理がそれ自体ワークストリームになり、二言語の二重化が
   両方の読者に不完全な経路を残している。

改善は「配布 → 計測 → 検索と契約 → UI と摩擦 → プロセス減量」の順で行う。
理由は単純で、配布は数日で直り最大のボトルネックであり、計測が無いままの
検索改善は当て推量になり、破壊的変更は利用者が増える前(0.x のいま)しか
安く済まないからである。

## 1. イケてないところ

コード監査の全文は長いので、ここでは判断を変える強度のものだけを引く。
番号は本計画の後段(§3)から参照される。

### W1. 検索: 製品の核なのに、品質を知る手段が無い

- 語彙検索は本文 ILIKE の包含判定で、語幹処理が無い。`revenues` は
  `revenue` に当たらない。tsvector はリポジトリに一度も現れない
  (`internal/store/search.go:243`)。当たらなかった検索は「知識の欠落」
  として記録されるので、実際にはレコールの欠陥なのにキュレーターへ
  「書くべき知識」として提示される — 計測が逆向きに壊れている。
- 日本語は保証付きの逐次スキャンで、корпус に線形。5,000 件で 16ms は
  既知(`internal/store/migrations/0016_search_text.sql`)、ROADMAP も
  認めるが、設計は未着手。主対象言語が最悪経路にある。
- スコアに頻度も文書長正規化も無い(BM25 不在)。「一致した重み付き
  フラグの和」なので、同語を含む文書間の順位は実質未定義。
- RRF 融合はストアが計算した較正済みスコアを捨て、その後で 0.002 の
  固定ナッジと `named` ブールで補正し直している
  (`internal/service/search.go:129-168`)— 二層が互いに打ち消し合い、
  オフライン評価が無いのでどちらも調整不能。
- 埋め込みは concept 一件に 1 ベクトルで、上限超過は静かに切り捨て、
  拒否時は半分に切って再送する(`internal/service/embed.go:263-297`)。
  長く価値ある文書ほど後半が意味検索から消え、誰にも通知されない。
- 評価: golden query セットも recall@k も無い。一方で cmd/ochakai には
  ドキュメント整合のテストが 33 本ある。**検証の文化が、検証がいちばん
  必要な場所にだけ届いていない。**

### W2. 改善ループ (C7): 測っているものが違う

- 「利用」は結果リストに載った回数である。`search_hit` は全ヒットに、
  `fetched` は pack が返した全件に記録される
  (`internal/service/search.go:44`、`internal/service/context.go:136`)。
  人もエージェントも何も選んでいない。sort=usage は「ランカーが良く
  出すものをランカーが良く出す」自己強化になる。
- カウンタは生涯累積で減衰しない。一年目に熱かった concept が今月熱い
  ものを永久に上回る。
- draft キューのバッジと一覧が別の集合を数える。バッジは status=draft
  を数え、一覧(usage 順)は絞り込みを掛けない
  (`internal/store/list.go:252` vs `:174-192`)。「12 件待ち」を開くと
  検証済みが混ざって出る。
- 唯一正直な信号 `report_outcome` は任意で、呼ばれているかを測る指標が
  無い。呼ばれなければ再検証フィードは空のままで「健全」に見える。
- miss は原文のまま無正規化で保存され、上位 10 件を完全一致でグループ
  する。同じ欠落が言い換えの数だけ分裂する。プレフィクスで絞れないので
  共有デプロイでは実質使えない。
- `failed` の生涯カウントがフィードの並び順を決めるため、直した過去の
  常習犯が、いま燃えている concept より上に出る(`internal/store/list.go:229`)。
- ループ自身のスループット(検証までの時間、キュー深度の推移、起草率
  対 検証率)は何も測られていない。**「測れるループ」が製品の中心主張
  なのに、収束しているかを測るものが無い。**
- usage の flush は 5 秒バッファ・失敗時破棄・20,000 件超は無言で
  ドロップ(`internal/store/usage.go`)。Cloud Run のスケールゼロ/
  ロールアウトで日常的に失われ、損失率は報告されない。

### W3. MCP: コンテキストを節約する製品が、常駐 3〜4k トークンを課す

- ツール記述 5,912 字 + プロパティ記述 5,519 字 + Instructions 約 1,700
  字。ツール**本数**は 6 に予算されたが、**バイト**は誰も数えていない —
  surface.md の思想(数えないものへ圧力が逃げる)がそのまま自分に起きた。
- `put_concept` の document スキーマは 1,449 字の一文
  (`internal/mcpserver/mcpserver.go:621`)。frontmatter は本来
  ネストした JSON Schema で表現でき、そのほうがモデルの理解も良い。
- `get_context` は毎回 340 字の同一文言を応答に付け、しかもそれは
  呼び出し元が指定した byte budget の外に乗る。
- `search_concepts` は検索と一覧の二能力を一スキーマに畳み、制約が
  スキーマで表現できず散文頼み。不正な組み合わせはエージェントの
  ターンを一つ無駄にする。CLI では 0062 が「一覧は検索ではない」として
  分割した当のものが、MCP では畳まれたまま残っている。

### W4. `get_context`: 看板の一呼び出しが最も遅く、契約を守らない

- ヒットごと・リンクごとに逐次 `Get` する N+1 で、一回の呼び出しが
  最大 60 ラウンドトリップ(`internal/service/context.go:83-109`)。
  重複除去も多様性制御も無く、同じメトリクスの言い換え 5 件が返り得る。
- budget は Concepts と Outline だけを縛り、Hits(最大 40 行)は外に
  乗る。スキーマは「応答中の知識の最大バイト」と教えているので、
  契約通りに窓を見積もったエージェントは溢れる。
- budget より大きい concept は rank 1 でも名前とバイト数だけになる。
  MCP 既定 12,000 バイトでは、最重要の一件が既定で保留される。

### W5. API とエラー: 統合する側の摩擦

- エラーは `{"error": "<英語の散文>"}` のみ。コードもフィールドも無く、
  時に 280 字の段落が返る。プログラムからは status code しか読めず、
  日本語化の経路も無い。
- openapi.yaml は 8 パスに 159KB。設計記録の再演である散文が大半で、
  コード生成器はまともな成果物を出せない。
- bundle エンドポイントは Accept とクエリで 6 通りの別物を返し、
  不正組み合わせの警備員関数が要る状態。ページングは検索と一覧で別の
  семантикスを持ち、規則は散文にしか無い。
- 書き込み結果の事実(unchanged / plan)がヘッダに乗っており、curl や
  生成クライアントから見えにくい。

### W6. オンボーディング: 4 本のクイックスタート、唯一楽な道は dev モード

- README は 4 つの入口(デモ / compose / CLI+gcloud / Terraform)を
  並べ、自分の知識を持てる道(Terraform、変数 27 個)が最後に来る。
- gcloud の最頻コマンド(application-default login)が唯一動かない
  もので、理由の説明は日本語のガイドの中にある。
- 摩擦ゼロの唯一の道は認証オフの dev モードで、そのまま staging に
  置かれる誘因が構造的にある。
- 自分のテーブルを入れる道は自前運用の Python ジョブ二本。
- `go test ./...` は DB 無しだと 477 本中 131 本を静かにスキップして
  緑になる。`go.mod` がパッチリリースを指名しており、GOTOOLCHAIN=local
  の企業 CI では最初の `go install` が落ちる。

### W7. Web UI: 信頼モデルの人間側が、一番弱い面に載っている

- 2,438 行の単一 HTML、うち 1,946 行がグローバル一名前空間の JS。
  実行可能なテストはゼロで、13 本のテストは HTML 文字列の grep。
- 手書き markdown レンダラは h1–h3 と平坦リストだけ。サーバーが
  エージェントに footnote を推奨し、同梱 UI がそれをゴミとして表示する
  — 面と面が矛盾している。
- role 属性 0 個、フォーカス管理なし、aria-live なし。企業の a11y
  調達審査は通らない。
- `lang="en"`、日本語文字列ゼロ。C8 を掲げる製品の人間面が英語のみ。
- インライン script 1,946 行のページに CSP が無い(public 姿勢は
  開けっ放しを設計で想定しているのに)。
- タブは Search と Review の二枚で、ループの推移(改善しているか)を
  見る画面が無い。

### W8. プロセスと文書: 装置がプロダクトより速く育った

- docs/ 709KB + ルート md 254KB 対 Go 807KB。コメント率 34% を足すと
  散文:コードが 1:1 を超える。あらゆる挙動変更が 4 ファイル以上の
  文書トランザクションになる。
- 数値天井が 5 系統(surface の 11 行 + CONTRIBUTING の 4 つ)あり、
  天井の上げ下げ自体を主題とするコミットがログに現れている。装置の
  目的(黙って増やせない)は達成されているが、maintenance が恒常費に
  なった。
- 設計記録は 80 冊。最大の 0064 は 64KB — 中央値の 15 倍、mcpserver
  パッケージ全体より大きい。行数 cap は 0065 以降にしか効かない。
- 索引は日英二重で、テスト 4 本が整合を警備する。一冊足すと最低
  4 ファイルを触る。
- 言語の分割が両方の読者を欠けさせる: 英語話者は docs/ で止まり、
  日本語話者は UI とエラーで止まる。

### W9. コード構造(緊急ではないが、将来の自分が払う)

- `Handler` 779 行、`restapi.go` 1,913 行、`client.go` 1,869 行、
  `newServer` 333 行。登録・検証・分岐が一体でユニットにできない。
- 同じ案内文(status / 型 / リンクの規則)が MCP 散文・OpenAPI 散文・
  マニュアルの三箇所で維持されている。コンパイラが見るのは一つだけ。
- 依存の鮮度・エラーラップ・マイグレーション規律・provenance モデルは
  平均を明確に上回る。問題は腐敗ではなく**比率**である。

### W10. 配布と認知

- GitHub 2 stars / 0 forks / issue 活動ゼロ。576 コミットの活発な開発が
  誰にも見えていない。
- 日本語圏の検索(「データカタログ AI エージェント MCP」)で出てくるのは
  Quollio・COMETA・OpenMetadata・dbt であり、ochakai は出てこない。
- MCP レジストリ・Smithery・Glama・mcp.so・PulseMCP・awesome 系、
  どこにも未掲載。競合は掲載をテーブルステークスとして扱っている。
- 「日本語検索がゼロ運用で動く」(OpenMetadata は kuromoji の手動
  インデックス構築が要る)という実利の差別化が、どこにも書かれていない。

## 2. 競合比較(2025–2026)

### 2.1 地図

| 系統 | 代表 | ochakai に対して持つもの | 持たないもの |
|---|---|---|---|
| ウェアハウス内蔵 verified-query | Snowflake Cortex Analyst + VQR、Databricks Genie、Google Conversational Analytics / BigQuery data agents | ゼロ統合、候補の自動提案(Snowflake はクエリ履歴から最大 10 件ずつ提案)、評価基盤(Genie は 500 問ベンチ内蔵)、semantic model への逆伝播 | 出口(知識はベンダーの中)、クロスウェアハウス、人間検証の台帳、解釈知識の型 |
| OSS カタログ | OpenMetadata、DataHub | MCP 込み全部 OSS(OpenMetadata)、日本でも草の根の記事網、コネクタと系譜 | エージェント起草→人間裁定のループ、日本語検索のゼロ運用(kuromoji/ES の手動設定が要る) |
| 商用カタログ | Atlan、Alation、Secoda、Coalesce | 「context layer」の物語と販路、SaaS の即時オンボーディング | キュレーションループは全て有償壁の中 |
| 日本市場 | Quollio(NTT Data 販路、MCP beta 2025-12)、COMETA(primeNumber) | 事例・販社・日本語サポート | OSS、セルフホスト、検証ループ |
| メモリ層 | mem0(51k stars, $24M)、Zep/Graphiti、Letta、Cognee | pip 一発、自動取得、ベンチマーク文化(LongMemEval) | 人間の裁定、チームの所有、no の記録 — 全員 LLM 内蔵・監査なし |
| ループの製品化 | Lightdash(agent 書き戻し→changeset→承認)、Dot(検証スペース)、Slite | 洗練された UI、チャット内の訂正導線 | git 可搬性、ツール非依存、解釈知識という単位 |
| 規格 | OKF v0.2(2026-07 に trust 段階を導入) vs OSI v1.0(Snowflake/dbt/Databricks ら 10 社、metrics) | OSI は連合の重さ | OKF は provenance と解釈の側を持つが採用実績が薄い |

### 2.2 2026 年のテーブルステークスで、ochakai に無いもの

1. **キュレーション済み知識への評価基盤。** Genie は 500 問 + 正解 SQL の
   ベンチを製品内で回す。ochakai の C7 は条件としては正しいのに、測定は
   競合の方が出荷している。
2. **コールドスタート。** Snowflake はクエリ履歴から検証候補を掘り、
   Cube は semantic model を自動起草する。ochakai の書き戻しループは
   成長を扱うが、空のストア問題への答えが無い。
3. **チャット内フィードバック→レビューの導線。** 悪い答えに 👎 を
   付けたらレビュー項目になる、が標準装備になった。ochakai は人が
   チャットを離れて UI へ行く。
4. **配布チャネルへの掲載。** 前掲 W10。
5. **OSI との相互運用。** metrics の側は連合規格に収斂しつつある。
   OKF→OSI の一方向 export を文書化するだけでも安い保険になる。

### 2.3 まだ空いている差別化(ここに張る)

1. **解釈知識が一級・検証済み・provenance 付き。** 「8 月はお盆で
   -15%」は Snowflake の VQR にも Genie の設定にも居場所が無い。
2. **検証ループが OSS の中核にある。** OpenMetadata に無く、商用勢は
   有償壁の中。ochakai だけが MIT で持っている。
3. **LLM 非内蔵・非実行の決定論。** 競合の従量課金($2/クレジット、
   ウェアハウス燃焼)に対し、$10/月の定額で監査可能。
4. **日本語検索がゼロ運用**(改善後は、の条件付き — W1 参照)。
5. **git に住む可搬な知識。** Lightdash の changeset も Genie の設定も
   製品の中に閉じる。知識がツールより長生きする物語は ochakai だけ。

**帰結: 「verified query store」はもうヘッドラインにできない。**
ヘッドラインは「エージェントが起草し、人が裁定し、どのエージェントにも
配られ、git に持ち出せる、チームの解釈知識」である。README と登壇と
各リスティングの一行目をこれに揃える。

## 3. 改善計画

順序の原理: 配布(数日で直る・最大の詰まり)→ 計測(無いと以降が
当て推量)→ 検索と契約(核)→ UI と摩擦 → プロセス減量(継続)。
破壊的変更は 0.x のうちに一括で払う。

### Phase 0 — 配布(1 週間以内・コード変更ほぼなし)

**P0-1. 載れる場所すべてに載る。** 公式 MCP レジストリ、Smithery、
Glama、mcp.so、PulseMCP、awesome-mcp-servers。あわせて `.mcpb`
バンドル(Claude Desktop ワンクリック)を release 資産に足す。
一度の作業で恒久の被発見性。効果測定: 参照流入と stars。

**P0-2. 登壇ファネルを閉じる。** 既存の Speaker Deck(データ
エージェントのナレッジカタログ、Socrates)から repo / ochak.ai への
明示的導線。次の登壇・記事は ochakai を名指しする。

**P0-3. 日本語の技術記事を 2 本。** Zenn に「Claude Code にデータ分析
ナレッジを持たせる(60 秒デモ→自前デプロイ)」、Qiita にハンズオン。
検索語「データカタログ AI エージェント MCP」で出てくる状態を作る。

**P0-4. README の一本道化。** 4 入口を「60 秒(公開デモ)→ 10 分
(compose)→ 本番(Terraform)」の一本の階段に並べ替え、§2.3 の
ヘッドラインを冒頭に置く。差別化 3 点(ループが OSS / 日本語ゼロ運用 /
git 可搬)を Why の先頭へ。

### Phase 1 — 計測を正す(〜1 か月)

**P1-1. 検索評価ハーネス。** examples/demo を種に日本語/英語混在の
golden query セット(質問→期待 concept)を作り、recall@k と MRR を
CI で回す(DB 付き job)。LLM 不要・決定論。以後の検索変更は数字を
付けて着地する。**これが Phase 2 の前提。**

**P1-2. ループの計器を直す。**
- `search_hit` を需要から外し、「取得された」(get / context で本文が
  渡った)と「載った」を分離する。
- 利用カウンタに時間窓(例: 90 日移動)を導入し、生涯カウントは参考値に
  降格。フィードの並びは鮮度加重にする(W2 の failed 恒久順位も同時に
  解消)。
- draft バッジと一覧の述語を一致させる。
- miss の正規化(小文字化・空白畳み込み・長さ上限)と、prefix 別の
  記録。
- `report_outcome` のカバレッジ指標(取得されたのに結果報告が無い
  concept 数)を stats に足す。ループの盲点を可視化する。
- usage flush の損失をカウンタで持ち、stats で報告する。
- ループのスループット(draft 起草数/検証数/検証までの日数)を週次で
  持つ。「ループが収束しているか」に初めて答えが出る。

**P1-3. MCP のトークン税を半減する。**
- `put_concept` の document を構造化スキーマに分解(frontmatter を
  properties で表現)。
- `get_context` の定型ヒントを応答から外し、Instructions に一度だけ
  置く。budget に Hits を含めるか、Hits を既定で薄くする。
- 記述文から設計経緯と重複を削り、6 ツール合計の常駐バイトに天井を
  設けて surface.md に十次元目として数える(この装置の思想をツールの
  バイトにも適用する)。
- `search_concepts` を search と list に分割する(0062 の CLI 判断を
  MCP にも適用)。ツール数 6→7 は、スキーマが素直になり誤用ターンが
  消える分で回収できる — 「本数は予算」の予算単位をバイトに改める。

### Phase 2 — 検索と get_context(1〜3 か月)

**P2-1. 語彙検索の作り直し。** 英語系は tsvector + ts_rank(語幹・
頻度・文書長を一度に獲得)、日本語は bigram GIN もしくは PGroonga の
採用を評価ハーネスで比較して決める。Cloud SQL は PGroonga を同梱しない
ため、既定は pgvector 側の再現率で補い、セルフホスト向けに PGroonga を
オプション化する構成が現実的。逐次スキャンの線形劣化(W1)をここで
終わらせる。ワイヤは変わらない — 破壊的でない。

**P2-2. スコアの一本化。** RRF に較正済み語彙スコアを捨てさせている
現状(W1)をやめ、単一のスコア合成に統一。0.002 ナッジと named
ブールはハーネスの数字で再設計する。

**P2-3. 埋め込みのチャンク化。** 長文 concept は分割埋め込みし、
検索は chunk→concept に集約。truncation が起きた concept を stats と
UI に出す(「この文書の後半は意味検索に載っていない」)。

**P2-4. `get_context` の修理。** バッチ取得(1〜3 クエリ化)、id
重複と近似重複の除去、budget 契約の是正(Hits 込みか、明示の別勘定)、
rank 1 が budget 超過のときは全文保留ではなく先頭抜粋 + 続きの住所を
返す。看板の一呼び出しを、最速かつ契約通りにする。

### Phase 3 — 統合と人間面の摩擦(1〜3 か月)

**P3-1. エラーモデル。** 追加互換で `code`(機械可読の安定識別子)と
`field` を error オブジェクトに足す。文言は短くし、長い教育的散文は
docs の恒久 URL へ。既存キーは残るので v1 凍結に抵触しない。CLI は
code を見て日本語メッセージを出せるようになる(C8)。

**P3-2. オンボーディングの一本道。** (a) `docker compose up` の 10 分
経路を「自分の知識を最初に持つ場所」として正面に置き、dev モードの
警告を UI 内バナーにも出す。(b) Terraform に最小プロファイル
(project / region / invoker の 3 変数で立つ defaults)を切る。27 変数は
上級ページへ。(c) 認証の「なぜ ADC が動かないか」を README の英語
一段落にも置く。(d) `go.mod` の patch 指名をやめ、`toolchain` 行で
解決する。(e) DB 無しテストのスキップ 131 本は、実行末尾に「store の
検証は走っていない」と一行で総括させる。

**P3-3. Web UI の作り直し(破壊的で構わない)。**
- ビルドなしは維持してよいが、単一ファイルはやめる: ES modules で
  分割し、レンダラ・ルーター・API クライアントを分離、Playwright の
  スモークを CI に足す(実行可能なテスト 0 本を終わらせる)。
- markdown は自前実装をやめ、監査可能な小型ライブラリを同梱(footnote
  矛盾の解消)。
- CSP を付け、a11y の基礎(role / aria-live / フォーカス管理)を入れる。
- 文字列テーブル化して ja/en 切替。既定は Accept-Language。C8 を
  掲げる製品の人間面が英語だけ、を終わらせる。
- Review に推移(キュー深度・検証率の時系列 — P1-2 の指標)を一枚
  足す。BI ツールにはしない: ループの計器盤はキュレーションの道具で
  ある。

**P3-4. コールドスタートの物語。** bigquery-catalog の日次投影を
「10 分で自分のテーブルが引ける」体験として README の階段に組み込み、
クエリ履歴から draft を起草する**クライアント側**ジョブの例を足す
(LLM を使うならユーザーのエージェント側で — サーバー非内蔵の原則は
変えない)。Snowflake の提案マイニングへの、ochakai 流の答えになる。

### Phase 4 — プロセスの減量(継続)

**P4-1. 天井の統合。** 5 系統の数値天井を「surface の一覧天井」と
「散文の総量天井」の二系統へ畳む。グリッド運用は総量側だけに残す。
天井コミットが日常から消えることが受け入れ条件。

**P4-2. 記録の減量。** RECORD-LINES の cap を全記録に遡及し、0064 は
「決定 300 行 + 付録(履歴)」に分割。英語 index は自動要約可能な
一行(タイトル + Status)に薄め、テスト 4 本を 2 本に減らす。

**P4-3. 言語戦略の宣言。** 「人間面(UI・エラー経由の案内・入門
記事)は日本語優先、契約面(openapi・README・CONTRIBUTING)は英語」
と一行で決め、docs の翻訳マトリクスをこれ以上広げない。

**P4-4. openapi の散文を降ろす。** 設計経緯の再演を恒久 URL への
参照に置き換え、159KB→生成器が食える大きさへ。コード生成の実用性は
C6(埋め込める REST)の一部である。

**P4-5. 三重化した案内文の単源化。** TypesGuide 方式(domain に一つ
置いて各面へ配る)を status / リンク規則にも広げる。

### 破壊的変更の束(一度で払う)

0.x の互換方針(minor で破壊可・最新のみサポート)のうちに、以下を
一つのリリースで払う: MCP の search/list 分割と応答スキーマ整理
(P1-3)、書き込み結果ヘッダの body への移設(W5)、usage 語彙の
再定義(P1-2)、Web UI 刷新(P3-3)。REST の追加互換変更(error への
code 追加)はいつでもよい。**まとめる理由**: 利用者が実質ゼロの今が
最安で、分割払いは告知コストを回ごとに払う。

### やらないこと(再確認)

LLM 内蔵・SQL 実行・コネクタ収穫・チャット UI・認可・secret は
引き続き持たない。競合調査はこの拒否がカテゴリの空白(= 差別化)で
あることをむしろ確認した。GCP-only も維持する — ただし「評価だけなら
ローカル compose で足りる」を正面に出す(P3-2)。

## 4. 受け入れ基準

| Phase | 指標 |
|---|---|
| P0 | 主要 5 レジストリ掲載、Zenn/Qiita 各 1 本公開、README 一本道化。90 日で stars とデモ利用の基線を取る |
| P1 | 検索ハーネスが CI で回り recall@k の基線が出る。stats がループのスループットと report_outcome カバレッジと flush 損失を返す。MCP 常駐バイトが半減し天井が数えられている |
| P2 | 日本語クエリの p95 がコーパス規模に対し準定数になる。ハーネスの recall@10 / MRR が基線から改善。get_context が契約通りの budget で N+1 を持たない |
| P3 | error に code が乗る。compose 10 分経路が README の正面にある。Web UI が分割され、スモークが CI にあり、ja で操作できる |
| P4 | 天井が二系統になり、記録一冊の追加で触るファイルが 4→2 になる。openapi から生成した Go/TS クライアントがそのままコンパイルできる |

## 5. この計画の限界

外部調査は 2026-08-08 時点の Web 検索に基づき、ochak.ai と
speakerdeck.com は本セッションの egress から到達できず未検証。競合の
機能は各社の公表資料ベースで、実地評価はしていない。W 系の指摘は
file:line まで確認済みだが、性能数値(16ms 等)はリポジトリ自身の
計測の引用である。配布(P0)の効果だけは事前に測れない — それでも
最初に置くのは、費用が最小で、失敗しても何も壊さないからである。
