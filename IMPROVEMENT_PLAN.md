# ochakai 改善計画(2026-08)

> 2026-08-08 時点の監査・市場調査スナップショット。以降に着地したものは
> CHANGELOG が正。

リポジトリとプロダクトの弱点の棚卸し、類似プロダクトとの比較、そして
改善計画。コードベースの全面監査と 2025–2026 の競合ランドスケープ調査を
もとに書いた。既存の設計上の制約(GCP-only、認可なし、表面の天井、
日本語マニュアル方針など)も聖域とせず、制約そのものを批判の対象に
含めている。破壊的変更を含む。

これは利用者向けマニュアルではなく、ochakai を変える人が読む文書である
(CHANGELOG・CONTRIBUTING と同じ面)。

## 総括

ochakai の守るべき核は監査でも競合調査でも揺らがなかった:
**(a) 人が検証したナレッジを (b) 持ち出せるオープン形式で (c) 中に LLM も
SQL 実行も持たず (d) 埋め込めるサイズで (e) 日本語ファーストで**配る組み
合わせは、2026 年時点でどの競合も持っていない。Snowflake VQR も
Databricks Genie の knowledge store も「検証済みクエリ+人のレビュー」
という中核の必要性をプラットフォームごとに実証しつつ、成果物を自社
ランタイムに閉じ込めている。ochakai の C1(資産は丸ごと出ていく)は、
彼らが構造的に真似できない一点である。

一方で、その核に**たどり着く前に評価者が脱落する**要因が積み上がって
いる。最大のものは三つ:

1. **英語の売り文句と日本語の運用手順が逆向き**(README は英語、
   デプロイ・設定・ポジショニングは日本語のみ)で、非日本語話者は
   ステップ 2 で壁に当たる。
2. **GCP 以外に安全に動かす道が一つもない**(アプリレベル認証ゼロ)。
3. **書き戻しループという看板機能を、デモでは体験できない**
   (demo.ochak.ai は read-only)。

そして製品の中身では、**到達できる者は何でも消せる**(認可ゼロ +
purge)、**バックアップが既定でオフ**、**blob が purge 後も残る**
(C1 と矛盾)、**観測性ゼロ**という、キュレーション済みナレッジを
「資産」と呼ぶ製品として看過できない穴がある。

最後に、**ガバナンスが製品より重い**。表面を数える仕組み一式
(surface.md 830 行 + surface_test.go 1,230 行 + CONTRIBUTING の天井
算術 ~350 行)は約 2,400 行あり、これは一人でメンテする 2 万行の製品に
対して逆転した比率である。仕組みの狙い(黙って増やせなくする)は良い。
コストが狙いを超えている。

## 1. イケてないところ(監査結果)

重大度は「採用・信頼への影響 × 直しやすさ」で付けた。証拠は
ファイル:行で示す。

### 1.1 採用の壁 — 重大

- **GCP-only はアプリ認証ゼロの帰結であり、逆ではない。**
  `internal/config/config.go:144-165` が姿勢の全空間で、認証は Cloud Run
  IAM に丸投げ。API キーも OIDC 検証も mTLS もなく、GCP の外では
  「全開の dev」か「read-only」しかない。secret-zero の原理は正しいが、
  JWKS URL に対する JWT 検証(`OCHAKAI_AUTH=oidc`)は secret を一つも
  持たずに実装でき、Cloud Run・Okta・GitHub OIDC を一つの経路で覆える。
  「第二の認証経路を維持できない」はメンテ側の論理で、利用者の論理では
  ない。
- **認可ゼロ = 到達できる者は verified の中核ごと purge できる。**
  `invoker_members = ["domain:..."]`(terraform.tfvars.example:12-16)を
  推奨しつつ、そのドメインの全員が `DELETE` + `purge=true`
  (`internal/store/store.go:1095` は本物のハードデリート)を打てる。
  provenance は事後にしか語らない。5 人を超えるチームには足切り要件。
- **read-only なデモでは、看板の書き戻しループを体験できない。**
  draft → 裁定 → verified という製品の中心を、Postgres を自前で立てず
  に触る方法がない。
- **実際の time-to-value は 3 コマンドではなく ~12 ステップ**で、うち
  6 ステップの文書が日本語のみ。

### 1.2 データ保全と運用 — 重大

- **バックアップが既定でオフ。** `terraform.tfvars.example:55-59` は
  `database_backups` も `deletion_protection` もコメントアウト。
  「$10/月」はデータ喪失既定で買った数字である。
- **purge がファイルの実体を消さない。** `internal/store/store.go:1086-1090`
  自身が「GCS は単調に増える。掃除は運用者が自分で書け」と認めている。
  C1(求められれば消える)と正面から矛盾し、GDPR/APPI の消去要求に
  応えられない。
- **観測性がゼロ。** `/health` だけ(`cmd/ochakai/main.go:259-262`)。
  /metrics も OTel も構造化リクエストログもなく、「検索が遅くなった」の
  診断手段が Cloud SQL への手作業アタッチしかない。「テレメトリなし」
  (外部送信なし)と「無計装」を混同している。
- **import が直列 N 回の HTTP ラウンドトリップ。**
  `cmd/ochakai/client.go:1464-1518`。5,000 テーブルの BigQuery カタログ
  投影(旗艦ユースケース)が ~10,000 直列リクエスト、5xx 一発で全体が
  中断、resume なし。サーバ側バルク import を断った判断とは独立に、
  クライアントのワーカープールは新しい表面ゼロで直せる。
- **レート制限・タイムアウトが薄い。** `ReadHeaderTimeout` のみ設定
  (`main.go:305-310`)。全世界に開いた demo が、全候補をマテリアライズ
  する検索(`store/search.go:318-326`)を無制限に受ける。

### 1.3 検索品質 — 中

- **スコアの同点が既知のまま未解決。**
  `searcheval_integration_test.go:105-115` 自身が「'why is revenue down'
  で 10 件中 5 件が同点、順序は任意」と書いている — README のヒーロー
  クエリで、である。予算固定(12,000 バイト)の `get_context` では
  任意の順序が「エージェントが何を見るか」を決めてしまう。
- **手作りの 6 定数 × 二段のスコアリング。** ストアのスコア
  (`internal/store/search.go:327-353`)を、サービス層の RRF
  (`internal/service/search.go:112-168`)が捨てて再ランクする。BM25 でも
  `ts_rank_cd` でもなく、どの定数もデータにフィットされていない。
- **eval が 14 ケース・レキシカルのみ・出荷コーパス上。** 既定構成
  (埋め込みオン)が無計測。fake encoder が無いのが原因と test 自身が
  言っている。
- **埋め込みは Vertex AI 一択**(`config.go:256-274`)。retry も明示
  タイムアウトもなく、一過性の 503 で黙ってレキシカルに退化(Warn ログ
  のみ)。
- **スニペット・ハイライト・「なぜこれが上位か」が無い。** 50 件の
  ヒットに断片が付かないので、エージェントは 1 件ずつ往復して確かめる。

### 1.4 API・エージェント DX — 中〜重大

- **エラーが `{"error": "<英語散文>"}` だけ。** `restapi.go:1638-1669`。
  machine-readable な `code` が無く、409 の二義(id 重複 / withdraw 対象
  なし)を散文の文字列マッチで区別するしかない。しかも散文は minor で
  変わってよいと compatibility.md が言う。**REST 凍結が一番の設計ミスを
  凍結してしまった**典型で、追加的例外(additive な `code` フィールド)
  を今決めないと固まり続ける。
- **MCP 6 ツールではループが閉じない。** `put_concept` の説明文自身が
  「別 id に良い draft を置いて人の昇格を待て」と指示するのに、MCP だけ
  のエージェントにはその後を見る術も、draft を元 concept に結ぶ術も、
  古い draft を取り下げる術もない。ホスト型エージェントの一次面が、
  ループの後半を欠いている。
- **ツール説明文が巨大。** `search_concepts` ~1,400 字、server
  instructions ~1,900 字。「スキーマはコンテキストから支払われる」と
  言いながら、6 本の密な散文は терse な 12 本より高くつく。
- **`GET /bundle/{path}` が 1 エンドポイントで 7 つの仕事**(spec 380
  行)。コンテントネゴシエーションが荷重を持ち、OpenAPI からのクライアント
  生成が壊れがち。
- **`Ochakai-On-Behalf-Of` の `*`** は「認証済み呼び出し元の自己申告を
  全部信じる」で、認可ゼロと組み合わさると、一つの不注意な埋め込み
  アプリが任意の人間名義で書ける。

### 1.5 Web UI — 中

- **サーバが自分の UI を配らない。** 非技術のキュレーター 1 人に
  ブラウザを渡すのに、第二の Cloud Run サービス + IAP + 2 つの環境変数
  という 4 点セットが要る(`main.go:264-282`)。
- **UI は英語のみ、マニュアルは日本語のみ** — C8 のターゲット利用者に
  対して正確に逆向き(`internal/webui/index.html:21`)。
- **2,438 行のフレームワークなし単一ファイル**をテストが文字列 grep で
  「読んで」いる。バルク verify・リビジョン diff・バンドル upload・
  キーボードショートカットなし、aria 属性 16 個。

### 1.6 貢献者体験とガバナンス — 重大(構造的)

- **フラグ 1 本足すのに触る場所が最大 12 箇所**(フラグ、生成 docs、
  surface.md の数、天井、openapi、apiclient、mcpserver か省略の弁明、
  Web UI か省略の弁明、日本語の設計記録 ≤300 行、索引、英語要約、PR の
  三つの問い)。良い提案を選別する仕組みではなく、提案者を選別する
  仕組みになっている。
- **80 冊の不変・ほぼ日本語の設計記録をコードが常時引用**しており、
  英語話者は `search.go` の ~30 箇所の根拠引用を一つも検証できない。
  英語要約は「権威ではない」と自認している。
- **REST/MCP/CLI/apiclient の四重同期が手作業**(CLAUDE.md が明文で
  そう指示)。openapi.yaml からの codegen が無いので、フィルタ項目の
  列挙が 4 箇所に生きている。
- **バス係数 1**、後継計画なし。SUPPORT.md は正直だが、E1 の重い貢献
  プロセスが係数 2 への道を狭めている。
- **互換性ポリシーの非対称**: 設計ミスを含む REST は永久凍結、エージェント
  が実際に依存する MCP ツール名・CLI フラグは minor で壊れてよい。

## 2. 類似プロダクト調査と比較

### 2.1 カテゴリ別の強豪と、あちらが上手いこと

| カテゴリ | 強豪 | イケているところ | ochakai との差分 |
|---|---|---|---|
| セマンティックレイヤー + MCP | dbt MCP(Fivetran 合併済)、Cube D3、Looker MCP | `uvx dbt-mcp` の一発起動、**ホスト型リモート MCP**(インストール不要)、メトリクス定義を変換と同じ repo の YAML で | 計算の定義はあちら、**判断(解釈・閾値・注意書き)はこちらだけ**。ochakai は実行しない代わりにベンダー非依存 |
| 検証済みクエリストア | **Snowflake Cortex Analyst VQR**、Databricks Genie trusted assets | エージェントが候補を提案→人が verify→モデル自体が改善される**閉ループがプラットフォーム内で完結**。2026-04 GA | ochakai の中核概念の必要性を最大手が実証。ただし全部**自社チャット専用でロックイン** — C1 の出口はどこにも無い |
| データカタログ | Atlan、OpenMetadata、DataHub、Select Star、国産 Quollio | **コネクタで初日からコンテンツが只**(自動収集)、リネージ、全員「context layer」を名乗る 2026 | 重量級(k8s 級 OSS か 5 桁ドル SaaS)。解釈ナレッジとレビューキューのループは商用ティア側か不在 |
| エージェントメモリ | mem0(~41k+ stars)、Zep/Graphiti、Letta | `pip install` + ホスト無料枠 + **自動抽出**でオンボーディング最速。OpenMemory MCP はローカルファースト + 内蔵 UI | 書いたものを誰もレビューしない・provenance なし。ochakai の「監査される記憶」はこのカテゴリの正確な裏面 |
| AI アナリスト OSS | Wren AI(~11k stars)、Vanna 2.0 | Docker ランチャー、質問→SQL ペアの examples ストア、週次リリース、Discord 1,700 人 | あちらは**エージェント本体**、ochakai は**他人のエージェントに食わせる側** — モデルの世代交代でコモディティ化しない、推論コストもない |
| コンテキスト基盤一般 | MCP Registry(~10,000 サーバ)、spec-kit、Obsidian/Notion MCP | markdown vault + MCP は「10 分で立つ最安の代替」 | vault には検証の状態・ループ・identity・到達性が無い(positioning.md の議論のとおり) |

主要ソース: dbt MCP(docs.getdbt.com/blog/introducing-dbt-mcp-server)、
Snowflake VQR(docs.snowflake.com/user-guide/snowflake-cortex/cortex-analyst/verified-query-repository)、
Databricks trusted assets(docs.databricks.com/aws/en/genie/trusted-assets)、
Cube D3(cube.dev/blog/announcing-cube-d3)、
mem0 OpenMemory(mem0.ai/blog/introducing-openmemory-mcp)、
Wren AI(github.com/Canner/WrenAI)、
Quollio INTEGRAL(prtimes.jp/main/html/rd/p/000000030.000090718.html)。

### 2.2 市場の向き

1. **MCP はテーブルステークス**になった(Linux Foundation 移管、本番
   運用 41%)。差別化は「どのコンテキストを、何トークンで」に移った —
   ochakai の「スキーマは予算」という姿勢はど真ん中にいる。
2. **検証済みクエリリポジトリが標準装備化**しつつあり、必ずベンダー
   ランタイムに閉じている。ochakai の出口(OKF 丸ごと往復)は唯一性が
   増している。
3. **Open Semantic Interchange v1.0**(2026-01、30+ ベンダー)が
   メトリクス定義の交換層になりつつある。OKF と OSI の関係を語れない
   のは近い将来の弱点。
4. 日本市場では Google Cloud(BigQuery + Looker)が言説を支配し、
   「AI に SQL を書かせない設計」系の実務記事が Zenn に出始めている —
   **市場が ochakai の主張の方向に教育されつつあり、この niche
   (セルフホスト・GCP ネイティブ・日本語ファースト・MIT)の国産 OSS は
   見当たらない**。国産の対抗は Quollio だがエンタープライズ営業モデル。

### 2.3 比較の結論

競合が上手いのは一貫して**「最初の 10 分」**である: `uvx` 一発、ホスト
型 MCP、無料枠、自動収集、Docker ランチャー。ochakai が上手いのは
**「3 年後」**である: 資産が人質にならない、監査可能、ベンダーの
チャットに縛られない。改善計画の主軸は、**3 年後の強みを守ったまま
最初の 10 分を競合並みにする**ことに置く。

## 3. 改善計画

各項目に、効く条件(C1–C8)/破壊的か/根拠(§1・§2 の番号)を付す。
フェーズ内は優先順。

### Phase 0 — 信頼の穴を塞ぐ(小さく、すぐ)

1. **`terraform.tfvars.example` の `database_backups` と
   `deletion_protection` を既定オンにし、費用例を書き直す**(§1.2)。
   ナレッジを資産と呼ぶ製品のコピペ初期値がデータ喪失既定なのは矛盾。
   10 分で直る。
2. **blob GC スイープを実装する**(§1.2)。purge 時に参照の消えた GCS
   オブジェクトを消す。C1(求められれば消える)を回復する。実装まで
   の間は README の refuses 表に消去の限界を明記(隠れた矛盾より宣言
   された制限)。
3. **`ochakai import` にワーカープール(`--concurrency`、既定 8)と
   resume を足す**(§1.2)。新しい表面は 1 フラグ、三つの問いに自力で
   通る。旗艦ユースケース(BigQuery カタログ投影)の 17 分待ち+一発
   中断を消す。
4. **エラー封筒に machine-readable な `code` を追加する**(§1.4)。
   additive なので `/api/v1` 凍結と両立し得るが、凍結ポリシーへの明文
   の例外を今決める。全エラーサイトに定数を割る。
5. **HTTP サーバの `ReadTimeout`/`WriteTimeout`/`IdleTimeout` と、
   public モード時の素朴なレート制限**(§1.2)。demo が最初の悪戯で
   落ちる前に。
6. **Vertex 呼び出しに retry + タイムアウト**、退化時は応答ヘッダで
   「今回はレキシカルのみ」と宣言(§1.3)。黙った品質低下を可視化。

### Phase 1 — 評価者の脱落を止める(採用)

7. **英語ドキュメントの生成翻訳**(§1.1, §1.6)。C8(日本語ファースト)
   は守りつつ、`docs/configuration.md`・`deploy/terraform/README.md`・
   `docs/positioning.md`・`docs/guides/mcp-clients.md` の 4 枚に
   「生成翻訳・非権威」と明記した英語版を**リリース時に機械生成**する。
   「鏡は古びる」問題は生成+マーク+CI 再生成で解く。手書きの鏡を
   禁じる現行方針とは両立する(生成参照ページの既存カテゴリに入る)。
   売り文句が英語で手順が日本語という逆向きを解消する。
8. **`OCHAKAI_AUTH=oidc`(JWKS 検証)を足す**(§1.1)。**破壊的では
   ないが方針変更**: 0003(GCP-only)の利用者向け帰結を削る。secret は
   引き続きゼロ(公開鍵検証のみ)。Cloud Run 経路は現状のまま既定。
   これで「GCP でなければ全開か read-only」が消え、失格要件が一つ減る。
9. **書けるデモ**(§1.1)。demo.ochak.ai の隣に、毎時リセットされる
   サンドボックス(`OCHAKAI_MODE=dev` + cron restore)を置き、README の
   クイックスタートを「draft を書く → レビューキューで見る」まで届か
   せる。看板機能が初めて 5 分で体験可能になる。
10. **`npx`/`uvx` ラッパーで MCP ブリッジを配る**(§2.1, dbt/mem0
    パターン)。`npx ochakai-mcp https://...` が Claude Desktop の設定
    一行で動く。Homebrew tap も、cross-repo PAT を理由に禁じた判断を
    見直す — secret-zero は**サーバ**の性質であり、メンテナの CI の
    性質ではない。
11. **seed-from-schema ブートストラップ**(§2.1)。
    `ochakai seed bigquery <dataset>` が INFORMATION_SCHEMA から
    テーブルカタログ entry の **draft** を生成してレビューキューに積む
    (LLM 不要・純メタデータ)。カタログ勢がコネクタで解く「初日空っぽ」
    問題を、ochakai の流儀(draft → 人の裁定)で解く。既存の
    examples/bigquery-catalog の手順をコマンド化するだけでもある。

### Phase 2 — 中身を競合水準へ(検索・ループ・DX)

12. **検索の同点を潰し、eval を ~100 ケース + fake embedder に拡張**
    (§1.3)。同点タイブレークは verified_at 降順 → usage 降順の決定的
    順序をまず入れ(任意よりまし)、その上で BM25(`ts_rank_cd`)への
    置き換えを golden set で A/B する。既定構成(埋め込みオン)が
    無計測という状態を終わらせる。
13. **MCP のループを閉じる**(§1.4)。`put_concept` に
    `supersedes: <id>` を足して draft と元 concept を結び、
    `get_concept` の応答に「この concept を継ぐ draft の有無」を載せる。
    ツール数 6 のまま、説明文が指示する行動を可能にする。
14. **ツール説明文のダイエット**(§1.4)。6 本合計のトークン数を計測して
    公表し(§2.1 の「context engineering」市場は MCP:6 という数字と
    実測トークンを聞く)、説明文を半分に刈る。
15. **検索応答にスニペット**(matched-term 前後 ~200 字)を足す
    (§1.3)。エージェントの 1 件ずつの確認往復を消す — コンテキスト
    予算の実質的な節約であり、C5/C6 に効く。
16. **引用の規約を文書化する**(§2.1, Zenlytic/Cube パターン)。消費
    側エージェントが「concept id + trust + verified_at」を答えに引く
    ための一段落を examples/claude-code/CLAUDE.md に足す。表面ほぼゼロ。
17. **`/metrics`(Prometheus)か構造化リクエストログ**(§1.2)。
    リクエスト時間・エラー率・DB プール・検索退化回数。外部送信なしの
    原則はそのまま。

### Phase 3 — 構造の負債(破壊的変更を含む)

18. **ガバナンスの半減**(§1.6)。八つの条件と三つの問いは残す(これは
    製品の背骨)。落とすのは: 行数天井(DOC-LINES/RECORD-CORPUS-LINES と
    その SLACK グリッド)、九次元の実数一致チェックの大半。
    surface_test.go を ~1,230 行から ~300 行へ。数える対象は REST・
    MCP・ENV の三つ(利用者が実際に払う三面)に絞る。
19. **openapi.yaml からの codegen**(§1.6)。apiclient とサーバの
    リクエスト/レスポンス型を oapi-codegen で生成し、四重同期の手作業を
    二重(REST 定義 + MCP)に減らす。散文コメントの文化はハンドラ側に
    残る。
20. **Web UI を日本語化し、サーバから配る**(§1.5)。C8 の利用者が
    読める UI にする。`OCHAKAI_MODE` に UI 同梱の姿勢を足し、第二
    サービス + IAP + 委譲設定の 4 点セットを 1 変数にする。2,438 行
    単一ファイルは、ビルドステップなしの制約を守るなら最低限テスト
    可能な分割(テンプレート + 関数単位ファイルの埋め込み)へ。
21. **互換性ポリシーの再均衡**(§1.6)。MCP ツール名と CLI の基本
    動詞に 1 リリースの deprecation 窓を導入する(エージェント設定は
    minor ごとに壊れてよい、は 2026 年の MCP 市場では通らない)。
    引き換えに、`/api/v1` 凍結に「additive フィールドの明文例外」を
    設ける(#4 の error.code が最初の適用)。
22. **OSI との関係を一段落書く**(§2.2)。Metric concept の frontmatter
    から OSI/MetricFlow 形 YAML への export を「curation の家はここ、
    交換層はあちら」という位置づけで検討する。実装より先に立場の表明。
23. **バス係数**(§1.6)。MAINTAINERS.md と「メンテナが消えたらどう
    なるか」の一段落(MIT + OKF 出口があるので実は答えやすい)。#18 の
    ガバナンス半減が、二人目が入れる前提条件でもある。

### やらないことは変えない

競合調査は「中に LLM を持たない・SQL を実行しない・チャット UI を
持たない・コネクタ収集をしない」という拒否が差別化の源泉であることを
裏付けた(§2.3)。この計画はどれも撤回しない。#11 の seed は収集では
なく draft 生成(人の裁定を通る)であり、#8 の OIDC は secret-zero を
破らない。変えるのは拒否の中身ではなく、**拒否の外側にある摩擦**である。

## 4. 実施順の提案

- **今週**: #1(backups)、#4(error code の方針決定)、#6(retry)。
- **今月**: #2(blob GC)、#3(import 並列)、#5(timeouts)、
  #7(生成翻訳の仕組み)、#12(同点タイブレーク)。
- **四半期**: #8(OIDC)、#9(書けるデモ)、#10(npx 配布)、
  #11(seed)、#13–#17。
- **議論から**: #18–#23(いずれも破壊的または方針変更。個別に設計
  記録が要る — ただし #18 が先に通れば記録は軽くなる)。
